# 交接文档 —— 崩坏学园2 小助手（bh2-maa）

> 这份是给「换新对话继续干活」用的速查。项目细节在 `README.md`，这里只记**当前状态、待办、和踩过的坑**。

---

## 一、环境（已确认可用）

| 项 | 值 |
| --- | --- |
| 项目路径 | `D:\bh2-maa` |
| 模拟器 | MuMu Player 12，装在 `E:\MuMu Player 12` |
| adb | **`E:\MuMu Player 12\nx_main\adb.exe`**（GUI 用的是这个）；`shell\adb.exe` 也能用 |
| 连接地址 | `127.0.0.1:16384`（必须带 `-s 127.0.0.1:16384`，因为还有别的设备） |
| 分辨率 | **1280x720 横屏**（所有 roi / 坐标都按这个写） |
| 游戏包名 | `com.miHoYo.HSoDv2Original`（官服） |
| 框架 | GUI：MaaFramework **v5.13.0** + MFAAvalonia v2.16.1；Python 侧：**maafw 5.13.0**。**两边版本必须一致**，见第二节 |

**⚠️ adb 会挂**：症状是 `cannot connect ... 10061 积极拒绝`。模拟器本身是好的
（`MuMuManager.exe info -v all` 显示 `is_android_started: true`），是 adb 后台服务没了。
修法：

```sh
"E:\MuMu Player 12\nx_main\adb.exe" kill-server
"E:\MuMu Player 12\nx_main\adb.exe" start-server
"E:\MuMu Player 12\nx_main\adb.exe" connect 127.0.0.1:16384
"E:\MuMu Player 12\nx_main\adb.exe" devices      # 应输出 127.0.0.1:16384  device
```

---

## 二、怎么跑

**GUI（MFAAvalonia）现在能用了** —— 2026-09-16 实测：启动正常、资源检测通过、
控制器按设备指纹连上模拟器（`127.0.0.1:16384`）。

> 之前记的「MFAAvalonia 用不了 / 资源加载失败」是**资源修好之前**的旧结论（那份报错日志
> 停在 09-15 23:41）。资源修好之后 GUI 那一步就通了 —— 真正卡住的是**再往后一步**：
> 界面一直停在「正在启动 Agent」，五分钟后报「任务运行失败：连接」。

### ⚠️ Python 的 maafw 版本必须和 GUI 自带的 MaaFramework 对齐（这条最省时间）

GUI 自带的框架是 **v5.13.0**（`gui/runtimes/win-x64/native/MaaFramework.dll` 里能翻到
字符串 `v5.13.0`），Python 侧的 `maafw` 包**必须装同一个版本**：

```sh
python -m pip install maafw==5.13.0      # 回滚：python -m pip install maafw==4.3.2
```

本机原本装的是 **4.3.2**，大版本对不上 → agent 走的是框架内部协议 → 连不上 →
`AgentServer.start_up()` 返回 **False**（老代码没检查返回值）→ `join()` 永久阻塞 →
**界面卡在「正在启动 Agent」，进程活着、CPU 几乎为 0**，要等五分钟重试耗尽才报失败。
这种"不报错只是不动"最难查，所以：

- 升级到 5.13.0 之后两端一致，实测命令行那条端到端跑通；GUI 侧同理（agent 是每次运行
  现拉的 Python 进程，**改完不用重启 GUI**，下一次点「开始」就会用上新版本）
- `agent/main.py` 现在**检查 `start_up()` 的返回值**，连不上就直接打印原因并退出，不再空等
- 升级前 API 逐项核对过，没变：`AgentClient` / `AgentServer.start_up` / `AdbController` /
  `Resource.post_bundle` / `Tasker.bind/post_task` / `Toolkit.init_option` 签名都一样；
  而且 `Resource.get_node_data` **v5 才真的有**，`tools/dump_nodes.py` 靠它读最终节点内容

**命令行替代方案**（GUI 出问题、或想写脚本时靠它，现在也依然可用）：

```sh
python tools/run_task.py                     # 列出所有任务
python tools/run_task.py 领取邮件             # 跑一个任务（自动套用默认选项）
python tools/run_task.py 重复刷关 --option 次数=3
python tools/run_task.py --node 主界面_就绪    # 直接跑节点
python tools/run_task.py --node 主界面_就绪 --no-agent   # 不拉 agent
```

它会：读 interface.json → 按 GUI 规则展开选项（select/switch/checkbox/input 四种，
`{占位符}` 按 `pipeline_type` 转 int/bool/str）→ 加载资源 → 连 adb → **拉起 agent 并连上** → 跑任务 → 逐节点打印命中情况。

**验证过一个成功的输出长这样**：

```
任务「领取邮件」-> 入口节点 日常_邮件
按默认选项展开出 7 个节点的 override
agent 已启动 pid=33496 identifier=bh2maa301d7efd
资源加载 OK
agent 连接: OK
模拟器已连接
执行结果: succeeded = True
   日常_邮件 / 进消息 / 领取 / 体力 / 收尾 / 回主界面   全部命中
```

其它检查工具：

```sh
python tools/check_env.py            # 自检（pipeline 引用、缺图清单）
python tools/validate_schema.py      # 官方 JSON Schema 校验
python tools/check_template.py 日常/xxx.png --live 6   # 模板打分（连拍看抖动）
python tools/get_gui.py --sync-only  # 改完 pipeline 同步到 gui/
```

---

## 三、GUI 里的任务结构（7 个）

```
启动崩坏学园2   → 游戏包名
使魔探险        → 使魔优先派遣
商店购物        → （无选项）
关卡战斗        → 日常模块 / 自动模式 / 使用双倍卷
领取奖励        → 领取奖励项目
领取邮件        → 邮件操作
重复刷关        → 刷取次数 / 自动模式 / 使用双倍卷 / 碎水晶
```

**「关卡战斗」的「打哪些」只剩**：虚轴之庭 / 使魔的爱 / 活动 BONUS（默认只勾 BONUS）

---

## 四、已完成并验证过的

| 模块 | 状态 | 备注 |
| --- | --- | --- |
| 启动游戏 | ✅ **冷启动实测跑通**（2026-09-16 修完链式调度 bug 后重验） | 标题页点「账号切换」锚点 + 中下方继续；游戏已在前台时跑它也不会重启，可以一直挂在队列第一位 |
| 领取邮件 | ✅ **端到端跑通**（succeeded=True） | 领取附件 / 领取体力 / 删除已读（「自动读邮件」没实测，见待办 2） |
| 使魔探险 | ✅ 端到端跑通 | 领取奖励 + 一键派遣 |
| 商店购物 | ⚠️ 链路跑通，但买的是**零时馈礼**不是零时之种 | 买完就下架 |
| 虚轴之庭 | ✅ 端到端跑通 | 战斗→活动→多元裂缝→虚轴之庭→出击→扫荡 |
| 使魔的爱 | ✅ 链路通 | 同路径，用「快捷战斗」扫荡 |
| 活动 BONUS | ✅ **导航链 + 进本 + 打完 + 结算全部实测跑通**；体力不足那步刚修完没重跑 | 见待办 1 |
| 领取奖励 | ✅ 端到端跑通 | 每日存在感 / 每周任务可分开勾 |

---

## 五、待办（按优先级）

1. **BONUS 已经跑通了，只剩「体力不足 → 继续下一轮」那条链没在真机上重跑** ——

   实测那一轮（`python tools/run_task.py 重复刷关 --option 次数=2`，体力 50）的节点日志：

   ```
   日常_活动BONUS             命中 Rect(18,12,245,45)          主界面
   日常_活动BONUS_开列表        命中                            点右上角活动名回列表（战斗导航落在子页了）
   日常_活动BONUS_进活动        命中 Rect(436,168,116,36)      活动列表里的「难度指数：B」
   日常_活动BONUS_点左上        命中                            地图第 2/2 页左上角 = BONUS 关
   日常_活动BONUS_选好友        命中 Rect(532,628,300,46)      1.0000，阈值 0.95 卡得很干净
   日常_活动BONUS_点好友        命中
   日常_活动BONUS_开战         命中 Rect(778,632,202,54)      ← 上一轮就是这一步掉链子的
   日常_活动BONUS_打完_自动     命中                            agent 的 bonus_battle 跑起来了
   日常_活动BONUS_收尾 / 日常_收尾
   ```

   连拍帧复核：结算页在**开跑后 64.5 秒**出现（战斗本身约 14 秒），
   `战斗结算.png` 稳定 1.0000 @ (566,112)，和 agent 的 `settle_roi [400,80,600,90]` 对得上。
   **确认了 BONUS 关是自动战斗**：放手不管 11 秒自己打完（挑战时间 11、受击 0、最大连击 27、积分 50、S 级）。

   **没跑完的一截**：体力只有 50，打完第 1 轮就剩 0；agent 想去刷第 2 轮，
   点了「再次挑战」（红的，我改的版本照样点）→ 弹出来的是**「补充体力」（花 60 水晶）**，
   **不是**交接文档原来记的「兑换双倍体力（免费）」—— 这个场景下根本没有免费那条路。
   因为 `use_crystal=false`，agent 拒绝购买、收工了（**没花水晶** ✅）。
   但**它当时没收工干净**：那个窗没关，模态窗挡住了后面的点导航，任务报成功、
   游戏却停在弹窗里出不来（见坑 31）。已经修好：
   - `_next_round` 拒绝买水晶时会**先点「取消」关窗**再收工
   - 收工统一走 `_finish()`：先检查/关掉体力窗，再点结算页「确定」
   - 「取消」的偏移 `[109, 199]` 已经**在真机弹窗上验证过**（点了窗确实关了）

   ⚠️ **下次要验的**：攒够体力再跑一次 `--option 次数=2`，
   确认第 2 轮能接着开起来、并且收工时不留弹窗（记得先看 `debug/agent.log` 里的 `[bonus]` 日志，
   现在加了 `-u`，日志不会再被缓冲吃掉）。另外顺便确认「兑换双倍体力」到底什么时候才会出现
   （大概要号上有双倍体力可换）。

   **体力恢复实测 ≈ 7 分钟 1 点**（48→49→50 花了约 13 分钟），一轮 BONUS 要 50 点 ——
   也就是说这个功能的验证成本挺高的，一次别浪费。

   BONUS 相关坐标/模板的实测汇总（结论和原来文档里的猜测不同）：

   | 项 | 实测值 | 结论 |
   | --- | --- | --- |
   | 结算页「再次挑战」 | 模板 156x45 命中 (560,591) | agent 的 `rechallenge_button [638,613]` **在按钮里** ✅ |
   | 结算页「确定」 | 模板 166x46 命中 (766,589) | agent 的 `settle_confirm [797,611]` **在按钮里** ✅ |
   | 结算页「战斗结算」标题 | 命中 (566,112) | `settle_roi [400,80,600,90]` 装得下 ✅ |
   | 好友列表第一格 | 实际 (234,182)-(694,329) | `support_slot [462,264]` **在格子里** ✅ |
   | 「开战」按钮 | 模板 202x54 命中 (778,632)，1.0000 | 在 `roi [600,580,600,140]` 里 ✅ |
   | 「选择助战好友」按钮 | 模板 300x46 命中 (532,628)，1.0000 | 点它正中进好友列表 ✅ |
   | 「补充体力」窗 | 标题命中 (574,229)、购买中心 (533,456)、取消中心 (749,456) | 买 `[-111,200]` ✅ / 取消 `[109,199]` ✅ |
   | 战斗界面一号位武器 | **没截到图** | BONUS 是自动战斗，这个坐标用不上；要确认就开战后 2 秒截一张 |

2. **「自动读邮件」未实测** —— 8 轮循环，坐标已按截图校准（第一封 `[200,190,720,90]`、
   返回箭头 `[15,165,90,50]`）。⚠️ **邮件详情页没有关闭键**，只有「删除」，退出要点左上角返回箭头。
   该选项默认不勾（`邮件操作` 里默认只有「领取附件」「领取体力」）。
   每轮「关 N」都有返回箭头，链路是自洽的；本轮开工时游戏卡在邮件详情页，是上一轮手动测试的残留。

3. **体力不够时任务是静默结束的** —— 体力 < 50（一轮要 50）时 BONUS 详情页的按钮会从
   「选择助战好友」变成「补充体力」，`选好友` 认不到、`翻页` 也被门槛图挡住了，
   于是整条链直接掉到 `收尾` 回主界面：**日志上看不出来是"没体力"，只像是没干活**。
   想让它说人话，得加个「没体力」判定节点（模板要从体力不足那屏裁，那屏的「补充体力」
   是**蓝字白底发光按钮**，和 `日常/补充体力.png` 那张灰字弹窗标题不是一回事）。

4. **「使用双倍卷」开关以前是坏的，修了但没实测** —— `interface.json` 里「重复刷关」的任务级
   `pipeline_override` 强行把 `日常_活动BONUS_双倍券` 打开了，而末位合并会把选项的 No 覆盖掉，
   等于勾不勾都跑双倍券。已经把那三行删掉。同时 `target_offset` 从 `[123,0,0,0]` 改成 `[196,0,0,0]`
   —— 复选框真实位置是 (1085,527)-(1113,560)、中心 (1099,543)，原来的 123 偏到了文字右边的空白处。
   ⚠️ 改完**没实测勾上之后真的会勾上**（要体力，且会消耗双倍券）。

5. **「使魔优先派遣」是空壳** —— 选项挂在界面上但 `pipeline_override` 是空的。
   要真按 S/A/B/C 排序，得给四个等级徽章各裁模板。**当前号上使魔探险全在进行中，截不到徽章。**

6. **「领取奖励」里的「吼姆的礼物」** —— ✅ 已接（入口 + 一键领取 + 关窗 + 收尾），
   并加进了 `领取奖励项目` 的默认勾选。还剩一件事：**那个「已领取奖励」弹窗还没见过**，
   所以 `日常_吼姆_关窗` 用的是现成那张模板，等真领一次才能验证。
   细节见下面第 38 条坑（侧边栏图标不能当模板、只能按死坐标）。

7. **「商店购物」应该买零时之种** —— 用户提过，但零时之种在商店哪一页还没找到。

8. **崩坏之塔：故意不做** —— 锁武器类型，自动打操作不来。

9. **「挂机 + 碎水晶」这个组合实际上跑不起来（已修到安全，但没修漂亮）** ——
   四个 `打完_*` 节点靠 `enabled` 挑一个，而「自动模式」和「碎水晶」是两个独立开关、
   override 是末位覆盖：**后合并的那个没法知道前一个选了什么**，所以拼不出全部四种组合。
   原来的写法是「自动模式=Yes」顺手把 `打完_自动_水晶` 也打开了，导致**默认设置
   （碎水晶=否）下 `打完_自动_水晶` 和 `打完_自动` 同时 enabled**，而 `开战` 的 next 里
   水晶版排在前面 → **没勾碎水晶也会花水晶**。已修的版本：`自动模式` 只开自己那一个不带水晶的，
   `碎水晶=No` 显式把两个水晶版关掉 —— 现在四种组合里三种正确，
   只有「挂机 + 碎水晶」会跑成「自动 + 碎水晶」（对 BONUS 无影响，因为 BONUS 本来就是自动战斗）。
   想彻底修干净得把两个开关合成一个四选一的 `select`。

---

## 五之二、用户 2026-09-16 报的 4 个问题（正在处理）

用户原话：「领取邮箱显示任务错误，关卡战斗有概率点歪，使魔之爱没有正常开始
（使用图标标记，但图标会每日轮替），"存在感"有时候不会主动切换去领取」。

### ① 领取邮件 → 任务失败　✅ **已定位已修（等实测）**

GUI 日志实证：`[02:02:18] 任务失败：领取邮件`（那一段跑了 28 秒 ≈ 20 秒超时）。

根因：**`日常_邮件_领取` 的 next 没有兜底节点**（违反坑 4）：

```json
"日常_邮件_领取": { "next": ["日常_邮件_领取_关窗", "日常_邮件_体力"] }
```

邮件里没有可领附件时，点「一键领取」不会弹「已领取奖励」窗 → `领取_关窗` 认不到；
而用户 GUI 里**「邮件操作」只勾了「领取附件」**，`体力` 节点是 `enabled: false`
→ 两个候选全废 → 死等 20 秒 → 任务失败。
（我在命令行按默认选项跑是好的，因为默认还勾了「领取体力」—— 这就是"你那儿好我这儿坏"的来源。）

已修：next 改成 `[领取_关窗, 体力, 收尾]`。

**新增工具 `tools/check_fallback.py`**：扫全项目，列出「next 里没有一个必然命中节点」的地方。
它扫出 48 处可疑 —— 注意**项目里 `收尾`（返回箭头）本身就是带识别的节点**，
所以"以收尾结尾"的链也算可疑，要人工判断；但正是它精准点出了出事的这一个。

**新增 `tools/run_task.py --case`**：复现 GUI 里勾了什么，例如
`python tools/run_task.py 领取邮件 --case 邮件操作=领取附件`。
没有这个就只能按默认选项跑，永远复现不出用户那条路径。

### ② 存在感的奖励有时候不领　✅ **已定位已修（等实测）**

用户 GUI 里「领取奖励项目」把两个 case 都勾了，而这两个 case **都在覆盖同一个 `next` 字段**：

```
case「每日存在感」→ 日常_存在感.next = [存在感_切页, 存在感_领取, 收尾]
case「每周任务」  → 日常_存在感.next = [每周任务_切页, 收尾]      ← 后合并的把前面整个盖掉
```

所以生效的只有后者：**流程只去切「每周任务」，存在感那边不领**；
而且面板本来停在存在感页签时，它还会把用户从存在感**切走**。

已经改成的写法（**选项只管 enabled，路由全放在 pipeline 里**）：
- pipeline 的 `日常_存在感.next` = `[存在感_切页, 存在感_领取, 每周任务_切页, 每周任务_领取, 收尾]`
- 两个 case 只保留 `enabled`，删掉所有 `next` 覆盖

这样三种勾法都对：只勾存在感 / 只勾每周任务 / 两个都勾（后者顺着
`存在感_领取.next` 和 `存在感_关窗.next` 里本来就有 `每周任务_切页` 继续跑）。

**新增工具 `tools/check_option_conflict.py`**：扫 "同一节点的同一字段被两处写"。
它同时挖出：
- 「领取奖励」的**任务级** override 把这 8 个节点全部强制 `enabled: true`，
  而任务级合并顺序在选项之后 → **「领取奖励项目」这三个勾选框其实完全无效**。
  （这跟「使用双倍卷」那个 bug 是同一类。**待办：把这 6 行删掉，只留入口 + 收尾**。）
- 「自动模式 × 碎水晶」的四组合问题（见待办 9）。

### ③ 关卡战斗「有概率点歪」　✅ **已改（BONUS 那条链）**

`日常_活动BONUS_点左上` / `点左上2` 原来是**死坐标点击 `(245,325)`**（上次只给 `翻页`
加了门槛图，漏了这两个）。活动一换、BONUS 节点在地图上的位置一变就点歪。

已改成用**金色 BONUS 标签定位**：`TemplateMatch 日常/活动_BONUS.png`，
实测地图页 **1.0000 @ (176,240)**，关卡卡片就在标签正下方 → `target_offset: [-4, 78, 0, 0]`，
阈值 0.95。这样跟页面在第几页、活动怎么轮替都无关。
**现场验证过**：按这个偏移点下去，BONUS 关卡详情页正确打开
（那一页上 `选择助战好友.png` 1.0000、`使用双倍券.png` 1.0000）✅

### ④ 使魔的爱没正常开始　✅ **已改＋已现场验证**

**（我先前的判断有一半是错的，这里更正）**：
我一开始扫了 123 张截图说「`日常/多元裂缝.png` 一次都没命中 → 这张模板废了」。
**其实模板没问题** —— 问题在**活动列表的滚动位置**：

| 列表状态 | 多元裂缝 卡片 | `多元裂缝.png` |
| --- | --- | --- |
| 滚到能看见卡片 | 完整 | **0.9997 @ (752,340)** ✅ |
| 停在顶部 | 被屏幕底边裁掉，标题露不全 | 0.43 ✗ |
| 滚过头 | 只剩副标题一条边 | 0.44 ✗ |

点「战斗」落在哪个滚动位置**每次都可能不一样**（取决于上次停在哪个子页），
所以这一步时灵时不灵 —— 这就是"没正常开始"和"有概率点歪"的观感来源。

**已做的三件事**（`使魔的爱` / `虚轴之庭` 两条链都改了）：
1. 入口 next 接上已经在 pipeline 里的 `_翻页`（就是那个 Swipe 滚动）——
   它以前只能从 `_回列表` 到达，而 `_回列表` 认的是关卡详情页的按钮，入口根本到不了。
   顺序：`[进多元, 回列表, 翻页, 开列表, 收尾, 逃逸]`。
2. 新增 `_开列表`（盲点右上角活动名按钮 `(1080,140,140,50)`）+ `_逃逸`（盲点首页导航，
   必然命中，兜到底再去做下一个模块）—— 补上「next 全不命中就死等 20 秒」这个老坑。
3. **点卡改用标签文字**（用户的提示：「使用图标标记，但图标会每日轮替」）——
   新裁 `日常/使魔的爱_文字.png`（129x30），`target_offset: [-9, 88, 0, 0]` 偏到图标卡上。

**现场验证过**：标签模板在聚合页 0.9980 @ (587,394)，按偏移点下去 → 关卡详情页开出来
（`快捷战斗.png` 1.0000 命中）✅。**没往下走 `快捷战斗`**（那一步要花体力）。

⚠️ 顺带记一条：**活动列表上的 `(1150,165)` 是一张卡，不是「活动名按钮」**
（活动名按钮只存在于各子页）。所以盲点它只有在「排在列表页的识别之后」才安全 ——
`_开列表` 就是这么排的，别把它提到前面去。

⚠️ **还没验的**：把 `关卡战斗` 的「日常模块」只勾 `使魔的爱` 跑一次完整链路
（会花体力扫荡一次，也正好推进用户那条每日任务「通过一次多元裂缝关卡『使魔的爱』」）。

---

## 六、踩过的坑（这些最省时间，改之前先看）

### 框架层

1. **`max_hit` 在这个框架版本完全无效** —— 实测日志里 `list=["测试_A","测试_B"]` 中已经命中过的节点照样被反复执行。
   所以「模块跑完回调度器、靠 max_hit 跳过」的写法会死循环。
   现在用的是**单向链式调度**：`日常_开始 → 日常_步骤2 → … → 日常_步骤7`。
   「刷 N 轮」也不能靠它，得在 agent 里自己数（BONUS 就是这么做的）。

2. **`custom_action_param` 是整体替换，不是深合并** —— 官方文档原话"直接替换，内部不会合并"。
   所以多个选项往同一个节点的 `custom_action_param` 上写，只有最后一个生效。
   BONUS 的解法是把「自动模式 × 用水晶」拆成**四个组合节点**，开关只负责 `enabled`。

3. **`roi` 必须把模板整个装下，差一点就全废** —— 「返回箭头」模板 80x68 贴在 (20,102)，
   roi 写 `[0,0,240,160]` 时下缘超出 10 像素，OpenCV 直接不匹配，分数只剩 0.29（全屏匹配是 0.99）。
   这种错**不报错不警告**，只是永远认不到。全项目的 `[0,0,240,160]` 都改成 200 高了。

4. **`next` 里全部不命中会死等到 20 秒超时**，把任务判失败。
   所以每个 next 列表末尾要有**必然能命中**的兜底节点。

5. **`next` 引用不存在的节点会让资源加载整体失败**（`Invalid next node name`），不是运行时才报。

6. **列表翻页别让节点在 next 里指回自己** —— 会一路滑到底还在滑。

### agent 层

7. **agent 连不上时不会报错，只会永久卡住 —— 而且原因常常是「版本不一致」** ——
   `AgentServer.start_up(identifier)` 连不上时不抛异常，只返回 **False**。
   老代码没检查返回值就直接 `join()`，于是进程活着、CPU 几乎为 0、界面上一直「正在启动 Agent」，
   要等五分钟重试耗尽才报「任务运行失败：连接」。**2026-09-16 实测根因：
   Python 的 `maafw` 是 4.3.2，GUI 自带的 MaaFramework 是 5.13.0，大版本对不上，
   agent 协议连不上。** 现在 `main.py` 会检查返回值并打印原因。
   > 早先这条坑记的是「`sys.argv[-1]` 拿到 `1`」那个参数解析问题 —— 那个写法确实有问题
   > （已修，见 `_pick_socket_id`），但**它不是这次卡住的原因**：MFAAvalonia v2.16.1 实际只传
   > 一个裸 identifier（日志里 `argv=['fmNmk2yF']`），老的 `argv[-1]` 也能拿到对的 id。
   > 排查 agent 连不上，**先对版本，再看参数**。

8. **`AgentClient.connect()` 必须先 `bind(resource)`** —— 否则立刻报 `resource is not bound` 失败。

9. **agent 的 identifier 别依赖自动生成** —— `AgentClient()` 不传参时 `client.identifier`
   有时返回 None，agent 拿到空 id 永远连不上。显式传随机串最稳。

### PI / GUI 层

10. **`switch` 类型的 `case.name` 必须是 Yes/No**（yes/y/no/n 也行，建议 Yes/No）。
    写成「开」「关」的话客户端认不出这是开关，会退化成普通下拉菜单。

11. **`input` 类型的字段别声明 `pipeline_type: "bool"`** —— MFAAvalonia 只接受 `true`/`false`，
    填 `1` 会报 `String '1' was not recognized as a valid Boolean`。
    声明成 `int`、填 `1`/`0`，agent 里 `bool(int(x))` 一样用。

12. **多个选项改同一个 `custom_action_param` 会互相覆盖**（见第 2 条）。

13. **裁模板前必须把截图缩到 1280x720** —— 用户给的截图常是 1467x825，
    直接按原图裁出来的模板尺寸是错的，后面所有坐标都是歪的。

### 游戏机制层（崩崩特有）

14. **导航栏选中的标签是白底**，按暗度做像素分析永远扫不到，点导航只能靠实测坐标
    （`654/766/879/991/1105/1218`，y=60）。

15. **「我在主界面吗」不能用左上角信息板判断** —— 任务面板、商店这些子界面顶部 UI 是同一套，
    在任务面板里也命中 0.97。改用「返回箭头在不在」（`inverse: true`）。

16. **活动子页的返回方式：点右上角那个活动名字** —— 虚轴之庭、崩坏之塔这类全屏子页
    **没有返回箭头**，导航栏也点不动。
    ⚠️ 按系统返回键是弹**「确认退出游戏」**，别按。

17. **点「战斗」导航不保证落在活动卡片列表上** —— 游戏会记住上次停在哪个活动子页。

18. **活动卡片每次轮换都换名字**，别拿活动名当模板。稳定特征是右上角的
    「难度指数：B」和「剩余时间：xx天」两个标签。

19. **这类刷刷刷活动的关卡入口就在活动页左上角**，固定坐标点过去就行，不用每次截图。

20. **「快捷战斗」是扫荡不是进战斗** —— 点了弹数量窗（默认 2 次，另有 Max），
    确定后直接出结算。「选择助战好友」才是手动进本。

21. **BONUS 关没有「快捷战斗」**，只能走「选择助战好友」手动进本（消耗体力 50）。

22. **邮件详情页没有关闭键**，只有「删除」，退出要点左上角返回箭头。

23. **「再次挑战」体力不足时会变红** —— 红色态认不出来（模板是正常态），
    流程据此判断该去处理体力弹窗了。

24. **体力不足弹的是「兑换双倍体力」**（兑换/取消，免费的双倍体力）。
    **水晶补体力是另一个窗叫「补充体力」**（购买/取消，花水晶）。
    两个窗按钮位置一样但标题宽度不同，`target_offset` 要各算各的：
    兑换 `[-168, 199]`、买体力 `[-111, 200]`。
    ⚠️ **补充（2026-09 实测）**：BONUS 结算页上体力为 0 时点「再次挑战」，
    弹出来的是**「补充体力」（花 60 水晶）**，**没有**免费那条路。
    所以别默认「体力不足 = 免费双倍体力」——「兑换双倍体力」什么时候才出现还没搞清楚
    （大概要号上真有双倍体力可换）。两套窗都得能处理：**拒绝了就点「取消」把窗关掉**（见坑 31）。

25. **`next` 里那个「必然命中」的兜底节点会把重试机会吃掉** ——
    MAA 的 `next` 逻辑是 `while(!timeout) { foreach(next); sleep_until(rate_limit); }`，
    也就是说候选节点本来会被反复重试最多 20 秒。**但只要列表里有个纯 Click 的兜底节点
    （比如 `收尾`），第一轮就命中，后面所有候选就再也不试了。**
    踩到的例子：`点好友 → [开战, 收尾]`，`开战` 那一瞬间没认出好友面板（怀疑面板还在滑），
    立刻掉到 `收尾` 回主界面，一轮白跑。**排查方向是「给动作加 `post_wait_freezes`
    等画面稳定」，而不是「把坐标再调准一点」。**
    （`post_wait_freezes: 1500` = 动作后等画面连续 1500ms 没大变化才去识别 `next`；
    官方文档的行文顺序：`pre_wait_freezes` - `pre_delay` - `action` - `post_wait_freezes` - `post_delay`。）

26. **同一个坐标在不同界面上可能是两个完全不同的东西** ——
    地图页的 (735,655) 是「下一页 ▶」，可 BONUS 关详情页的同一个点是「选择助战好友」。
    `翻页` 原本是纯按坐标点，结果在详情页上等于点了进本按钮，**直接把战斗开起来了**。
    修法：给动作加**门槛图** —— 先把 ▶ 裁成模板（`日常/活动_翻页.png`），
    认到才点，`roi` 收在页码那一小片。**凡是"按坐标点、不带识别"的节点，
    都要问一句：这个坐标在别的界面上会是什么。**

27. **按钮文案会变，模板就会假阳性** —— BONUS 详情页底部那个按钮，体力够时是
    「选择助战好友」，体力不够时变成「补充体力」。而 `选择助战好友.png` 在**错的那屏**
    上照样有 **0.9376** 分（默认阈值 0.85 → 认了），点下去白弹一个花水晶的窗。
    真按钮永远是 **1.0000**（静态图，像素级一致），所以**阈值提到 0.95** 就干净切开了。
    教训：**阈值不是拍 0.85 就完事，得拿"最像它的那张错图"量一下分数**，
    量法见 `tools/check_template.py --shots <把所有截图放一个目录>`。

28. **体力 < 50 时别在地图页/详情页乱点那一排按钮** —— 详情页底部的按钮变成
    「补充体力」，点它弹的是**花 60 水晶换 205 体力**的窗（`补充体力.png` 那张
    灰字标题就是它），不是免费的「兑换双倍体力」。免费那条路只在**结算页点「再次挑战」**
    才会出现。所以自动流程里"体力不够"必须靠"能不能认到按钮"来判断，不能靠"点了看看弹什么"。

29. **活动关地图的 BONUS 关在第 2/2 页的左上角，第 1/1 页那个位置是空的** ——
    所以 `点左上` 那个固定坐标 (245,325) 在第 1 页上是**点空**（安全，什么都不会发生），
    流程靠 `翻页` 翻到第 2 页再点一次。这条链是自洽的，不用怀疑它。
    另外：进活动卡片时地图**总是从第 1/2 页开始**，而打完一局退出来会回到第 2/2 页。

30. **`战斗` 导航不保证落在活动列表页，再点一次也没用** —— 游戏记住的是**上次停在哪个
    活动子页**（比如「休诊时间」关地图），点「战斗」直接回到那个子页，**连点两次也是原地不动**。
    回列表唯一的办法是点**右上角那个活动名按钮**（休诊时间/多元裂缝…位置一样，在
    `(1080,140,140,50)`）。这就是 `日常_活动BONUS_开列表` 这个节点的用途：它排在 `进活动`
    **后面**当兜底 —— 已经在列表页时 `进活动` 先命中，它就不会执行。

31. **agent 里点了会弹窗的按钮，弹窗一定要自己关掉** ——
    体力不足时点「再次挑战」会弹**模态**的「补充体力」窗。`use_crystal=false` 时不买是对的，
    但**光 return False 不行**：窗还开着，后面 pipeline 的 `收尾` 点导航栏会被模态窗吃掉，
    于是任务报 `succeeded=True`、日志漂漂亮亮，**游戏却卡在弹窗里出不来**（实测踩到）。
    现在的处理：拒绝买就**点「取消」**，收工统一走 `_finish()`（先查/关体力窗，再点「确定」）。
    **通用教训：凡是会弹窗的分支，都要写清楚"这个窗谁来关"。**

32. **注释掉的日志等于没有日志** —— agent 是用 `subprocess.Popen(stdout=文件)` 拉起来的，
    子进程的 stdout 是**块缓冲**，而跑完是 `kill()` 掉它 —— **缓冲区里没冲出来的 print 全丢**，
    `debug/agent.log` 里只剩启动那一行（排查时白等一场）。
    修法：`python -u` + `PYTHONUNBUFFERED=1`（`tools/run_task.py` 里已经加上了）。

33. **坑 4（next 全不命中会死等 20 秒）平时看不出来，被选项关掉候选的时候才炸** ——
    `日常_邮件_领取.next` 原本是 `[领取_关窗, 体力]`，看着有俩候选挺稳；
    可 `体力` 是被「领取体力」这个选项控制的，用户只勾了「领取附件」→ 它 `enabled: false`；
    而 `领取_关窗` 需要「已领取奖励」弹窗出现（邮件里没附件就不会弹）
    → 两个全废 → 20 秒超时 → `任务失败：领取邮件`。
    **教训：读 pipeline 只能看见候选，看不见"这一刻哪些候选是关的"。
    要按用户的勾法复现 —— 用 `python tools/run_task.py <任务> --case 选项名=case名`，
    再用 `tools/check_fallback.py` 扫一遍。**

34. **两个 case 抢同一个 `next` → 复选框形同虚设**（坑 2 / 坑 12 的同类，
    但那里抢的是 `custom_action_param`，这里抢的是 `next`）——
    踩到的是「领取奖励项目」：「每日存在感」把 `日常_存在感.next` 设成
    `[存在感_切页, 存在感_领取, 收尾]`，「每周任务」又设成 `[每周任务_切页, 收尾]`，
    用户两个都勾 → 后合并的赢 → **存在感的奖励永远不领**。
    **正确写法：选项只管 `enabled`，把路由（`next`）全部放进 pipeline。**
    检查工具：`python tools/check_option_conflict.py`。

35. **任务级 `pipeline_override` 会盖掉选项**（「使用双倍卷」和「领取奖励项目」都是这个病）——
    合并顺序是「先各选项的 case，最后任务级」，所以任务级里写了的字段，
    选项再也改不动。踩到两次：
    - 「重复刷关」强行 `日常_活动BONUS_双倍券.enabled = true`
    - 「领取奖励」把 8 个存在感/每周任务节点全部强行 `enabled = true`
      → **「领取奖励项目」那三个勾选框完全无效**
    **任务级只该写"入口 + 收尾"，其余交给选项。**

36. **模板是会过期的，而且过期是静默的** —— `日常/多元裂缝.png` 现在
    **123 张历史截图里一张都认不到**（最高 0.46）。节点不报错，只是永远不命中，
    整条链安静地掉到收尾（表现为"这个模块没跑"）。
    定期体检：`python tools/check_template.py <模板> --shots <一把截图目录>`，
    或者把 `assets/resource/image/_raw` 里最新的图丢进去扫一遍。

37. **用户给的截图，先确认比例再裁模板**（坑 13 的续集，栽了三次）——
    **标准动作：先量图片宽度，按 `1280 / 宽度` 缩放，再拿去 `check_template.py` 打分，
    分数到 0.95 以上才允许用来裁模板。**
    三次实证：
    - 「使魔的爱」标签图 160x49：原样 **0.6017**，缩回 1280 后 0.9871 ✅
    - 「多元裂缝」标题图 248x64：原样 **0.4874**，缩回 1280 后 0.9899 ✅
    - 自己从 1280x720 原生截图裁的：直接 1.0000 ✅
    **更好的办法是别用窗口截图**：让用户把游戏停在那一屏，自己用
    `adb -s 127.0.0.1:16384 exec-out screencap -p` 抓原生 1280x720，像素级干净。
    窗口截图缩放插值会带模糊，裁出来的模板天生比原生低一档。
    （另：用户截图是 1467x825 时，缩放系数是 `1280/1467 ≈ 0.8725`。）

38. **侧边栏图标不能当模板**（但位置固定，可以按死坐标点）——
    主界面左侧那排图标是**半透明圆角块，底下的场景会动**，像素内容跟着变：
    同一块内容在三张主界面截图上分数是 **1.0000 / 0.8299 / 0.9025**（抖动 0.17），
    阈值 0.95 卡不住。**但位置完全固定**（三张图上都命中同一个 (24,324)）。
    → 结论：侧边栏入口一律**按死坐标点**（现有 `日常_存在感` 的 `[15,123,60,60]`
    就是这个路子），别费劲裁模板。第三个图标（吼姆的礼物）实测占 **(24,307)-(80,368)**，
    它右上角还有个时有时无的蓝点角标 (68,303)-(86,322)。

39. **「我在主界面吗」不能用玩家信息板判断 —— 已换成侧边栏图标**（坑 15 的完整闭环）——
    原来 `通用/主界面.png` 裁的是**左上角玩家信息板**（含**用户自己的头像**），三个问题：
    1) **不通用**：别人装的号头像不一样，永远认不到；
    2) **子页上也有**：子页顶部 UI 是同一套，实测邮件页 0.9704、吼姆页 0.9704、任务面板 0.97
       → 子页被误判成主界面 → 去点侧边栏坐标 → 后面全不命中 → **死等 20 秒判失败**。
       **用户实测的「商店购物」失败就是这么来的**（前一个任务把游戏留在邮件页，
       商店的入口照样判定"在主界面"，点了商店导航但点空，然后 `进礼包`✗ `切吼美`✗ `收尾`✗
       → 26 秒后任务失败）；
    3) 等于把**隐私**（头像）打包进资源。
    **现在的做法**：换成**左侧侧边栏第一个图标**（白色文档图标，通用、不含个人信息）。
    实测判别力（阈值 **0.75**，中间有 0.25 干净间隔）：
    主界面 1.0000 / 0.9134 / 0.8587；邮件页 0.5533、吼姆页 0.4875、活动列表 0.6001、
    任务面板 0.4742、开战页 0.6136。
    ⚠️ 侧边栏图标是半透明块、底下场景会动，主界面侧分数会抖到 0.86 —— **阈值别写 0.85**。
    ⚠️ **试过但不成**：`And(all_of=[信息板, 返回箭头 inverse])` —— 实测 `And` 里无论是
    节点引用还是内联写法，**`inverse` 都不生效**（在邮件页照样命中）。
    ⚠️ **还没做**：入口缺「先退回主界面」这一步。现在门卫严了，游戏不在主界面时
    入口直接不命中 → 任务会在入口处超时失败（比误点干净，但还是失败）。
    要真正解决，得给每个任务的入口包一层：`入口节点.next = [模块节点, 回到主界面_退一步]`，
    `回到主界面_退一步.next = [入口节点, 回到主界面_退一步]`（退一步→重试，直到门卫放行）。
    **这是下一件该做的事。**
    （附带一条：`通用/返回箭头.png` 在「吼姆的礼物」这种浅色页上只有 **0.8688**，
    离阈值 0.85 只剩 0.019 余量 —— 收尾链靠它的地方都别把阈值往上提。）

43. **入口要「先点一次首页」，不能只靠"我在主界面吗"的门卫**（✅ 已做，实测通过）——
    门卫严了之后暴露的新问题：`社交`/`商店`/`装备` 这些是**导航栏的顶层页** ——
    既不是主界面、**也没有返回箭头**，所以只靠"点返回箭头"的回退链在这些页面上会卡死。
    实测：游戏停在**社交页**时跑 `商店购物` → 入口✗ → 退一步✗ → 链断 → 任务失败。
    **做法**（每个任务三个节点，每层都有上限，不会死循环）：
    ```
    T_入口     DirectHit            → [T_回首页, 日常_收尾]
    T_回首页    点首页导航(纯点击)     → [模块, T_退一步, 日常_收尾]      max_hit 3
    T_退一步    认返回箭头点击         → [T_入口, 日常_收尾]              max_hit 10
    ```
    入口先点一次首页（在导航栏页面上必然有效；已经在主界面时点了也无害），
    再走模块；模块门卫还没过就退回一步重试。
    **实测**：从社交页跑 `商店购物` → `入口 → 回首页 → 日常_商店 命中(28,136) →
    进礼包 → 切吼美 → 翻页 → 购买 → 确认 → 确定 → 收尾`，`succeeded=True` ✅
    （六个任务的 `entry` 都已指向新的 `*_入口` 节点。）

44. **「重复刷关」和「关卡战斗 · 活动 BONUS」是同一套逻辑** ——
    同一条链、同一批节点、同一个 agent 动作（`bonus_battle`），改一处两边都受影响。
    区别只有两个选项：
    - **刷取次数**：关卡战斗那边固定"一直打到没体力/双倍体力用完"；
      重复刷关用 `刷取次数` 指定轮数
    - **碎水晶**：体力不足时是否花水晶补体力 —— **只有重复刷关有这个开关**

45. **①型领奖弹窗（「已领取以下奖励」）已实测出现过** ——
    跑 `领取奖励` 时 `日常_存在感_关窗` 命中 `Rect(552,179,214,33)` ✅，
    证实两种弹窗都真实存在，拆成两个节点分别处理是对的（别合并）。

46. **给 Windows 写的 `.bat` 必须用 GBK 编码，外加三个 cmd 坑** ——
    - **编码**：cmd.exe 按系统 OEM 代码页（中文 Windows = GBK）读 `.bat`，
      用 UTF-8 存的中文会被解析成乱码，实测报 `& was unexpected at this time`，
      脚本根本跑不起来。所以 `启动小助手.bat` / `推送更新.bat` 都是 **GBK + CRLF**。
    - **带空格的路径**：`set ADB="E:\MuMu Player 12\nx_main\adb.exe"` 会把引号一起存进变量，
      再拿去 `for /f` 里用会被拆断（报 `'E:\MuMu' is not recognized`）。
      正确写法 `set "ADB=值"`，用的时候才加引号 `"%ADB%"`。
    - **等待**：别用 `timeout /t` —— 没有控制台时（输出被重定向）它会报
      `Input redirection is not supported` 然后卡住；用 `ping -n 6 127.0.0.1 >nul` 代替。

47. **发布前务必扫一遍"有没有把隐私提交进去"** —— 这次真栽了：
    `assets/resource/image/通用/主界面.png.bak_personal`（含玩家头像/昵称/等级的旧模板）
    被 `git add -A` 带进仓库、**并且推到了公开仓库**。
    处理：`filter-branch` 从全部历史里清掉 → 删 `refs/original/*` →
    `reflog expire --expire=now --all` → `gc --prune=now` → 强推；
    并把 `*.bak_personal` / `*.bak_*` 补进 `.gitignore`。
    **教训：加 gitignore 要连"备份文件"一起挡，别只挡目录**（当时只挡了 `dist/` 和 `_backup_*.json`）。

42. **发布给别人用时，模板图里的隐私要清一遍** ——
    踩过：`通用/主界面.png` 裁的是玩家信息板（**含用户头像**），
    既导致别人认不到（不通用），也等于把隐私打包进资源。
    **发布前扫一遍**：roi_hint 落在左上角信息板区域的模板都要人工看一眼。
    打包工具 `tools/package.py` 会自动跳过 `image/_raw/`（那是原始整屏截图，
    又大又含隐私），发布流程和检查清单见 **`PUBLISH.md`**。
    ⚠️ `assets/interface.json` 的 `github` 字段是 MFAAvalonia「更新资源」的源 ——
    **模板仓库的地址必须改成自己的**，否则别人点更新会去查 MaaXYZ 的模板仓库。

40. **领奖弹窗有【两种】形态，只写一种就是坏的**（2026-09-16 实测，这批关窗节点一直是坏的）——
    | 形态 | 标题 | 怎么关 |
    | --- | --- | --- |
    | ① | 「已领取以下奖励」 | **没有确定按钮**，底部写「(点击空白处关闭)」→ 点屏幕左侧空白 |
    | ② | 「奖励领取」 | **底部有「确定」按钮** → 点按钮中心 (664,476) |
    原来 5 个关窗节点（邮件领取/邮件体力/存在感/每周任务/吼姆）**只写了 ① 的写法**，
    于是 ② 式弹窗一出现就关不掉，**模态窗把后面所有点击全吃掉** ——
    表现出来就是"领了没反应 / 任务报错 / 后面整条链不动"。
    **已改成每种关窗拆两个节点**：
    - `X_关窗` —— ① 式：认 `日常/已领取奖励.png`（roi 320,100,640,420），点空白 `[40,590,60,60]`
    - `X_关窗_确定` —— ② 式：认 `日常/奖励领取_确定.png`（roi 400,250,520,400），
      **点识别到的框中心**（这样弹窗高度随奖品数量变化也不会点空）
    两个节点互相出现在对方 next 里、并且自环（一次可能连弹几个）。
    ①式的模板上一轮实测过 1.0000/关闭后 0.3164；②式的模板本轮实测 1.0000
    （在「补充体力」窗上 0.5746、开战页 0.4449，不会误认）。

41. **GUI 实例配置里，只有"勾选"需要手改，override 不用管** ——
    `gui/config/instances/*.json` 的 `TaskItems` 里两样东西：
    - `selected_cases`（用户勾了什么）→ **interface.json 新加的 case 不会自动勾上**，
      所以界面上看不到、跑起来也不执行。要补就得改这里。
    - `pipeline_override`（派生出来的）→ **GUI 重启时会自己按 interface.json 重算并存回**，
      实测重启后那份只剩任务级的两三个节点，正是 interface.json 里的样子。
      所以别去手工维护它（我一开始以为要重算，白折腾一轮）。
    工具：`python tools/refresh_gui_config.py [--add 任务=选项名:case名] --write`，
    会清掉失效的 case 名并把新 case 补进勾选。
    ⚠️ 改 `interface.json` 之后**记得重启 GUI**（它在启动时读 interface.json）。

---

## 七、GitHub 发布与推送（2026-09-16 起）

| 项 | 值 |
| --- | --- |
| 仓库 | **https://github.com/MuYangZhiJun/Maabhxy2**（已公开，主分支 `main`） |
| 更新源 | `assets/interface.json` 的 `github` 字段 —— MFAAvalonia 的「更新资源」读这个 |
| 怎么推送 | **双击根目录「推送更新.bat」**，或 `python tools/push.py -m "提交信息"` |

**为什么单独包了个推送脚本**：这台机器到 GitHub 的连接**时通时断**，
实测推一次重试了 6 次才成功（`Connection was reset` / `Failed to connect ... port 443`）。
`tools/push.py` 会：确保 `http.version=HTTP/1.1`（HTTP/2 在国内容易被重置）→ 暂存并提交
→ 最多重试 10 次（间隔 20 秒）→ 最后比对远端和本地的提交哈希是否一致。

**认证**：GitHub 早就禁用了"账号密码推送"，必须 token 或 OAuth。这台机器的凭据管理器（GCM）
走的是 **OAuth** —— 第一次推送会弹浏览器授权一次，之后不用管。
凭据里还留着一条旧的 `GitHub - https://api.github.com/MuYangZhiJun`
（`cmdkey` 删不掉带空格的 target，要在「凭据管理器」图形界面里删）—— 不影响 git 推送。

**历史**：远端是**单一干净提交**（`524a66d` 起）。当初本地是浅克隆（缺上游对象，
推上去报 `did not receive expected object`），所以把历史重做成一个提交；
旧的 main 用 tag `backup-before-clean` 留了保底；远端另有个早前的 `test` tag。

**发版流程**见 `PUBLISH.md`（发版前记得把 `assets/interface.json` 的 `version` 往上加，
MFAAvalonia 靠它判断有没有新版本）。

---

## 八、2026-09-16 这次会话干了什么（给下一轮快速接手）

按顺序：

1. **校准环境**：adb 会挂（`kill-server`→`start-server`→`connect`）；确认模拟器 1280x720。
2. **打通 GUI**：MFAAvalonia 之前"用不了"的旧结论是错的（那是资源修好之前的日志）。
   真正的坑是 **Python 的 `maafw` 4.3.2 与 GUI 自带 MaaFramework 5.13.0 版本不一致**，
   agent 连不上、界面卡「正在启动 Agent」。装了 `maafw==5.13.0` 后通了；
   `agent/main.py` 现在会检查 `start_up()` 返回值，连不上直接报错退出（不再空等）。
3. **修 BONUS 链 + 补工具**：`tools/crop.py`（裁图带网格）、`tools/burst.py`（连拍翻帧）、
   `tools/dump_nodes.py`（看 override 最终生效值）、`tools/pick_template.py`、
   `tools/template_health.py`、`tools/check_fallback.py`、`tools/check_option_conflict.py`。
4. **修了用户报的 4 个问题**（详见「五之二」）：领取邮件缺兜底、存在感被选项覆盖、
   BONUS 点歪、使魔的爱进不去。
5. **补了「吼姆的礼物」**（入口 + 一键领取 + 接进「领取奖励」），并**发现关窗节点一直是坏的**：
   领奖弹窗有两种形态，原来只处理了一种（详见坑 40）。
6. **主界面判定换成侧边栏图标**（原来是玩家信息板，含隐私、且子页会误命中 → 任务失败）。
7. **入口加「先点首页 + 回退重试」**（坑 43），六个任务的 entry 都改了。
8. **发布**：仓库推上 GitHub、README 改成对外介绍页（含 AI 代工声明）、
   `tools/package.py` 打包、`PUBLISH.md` 写发布流程、`tools/push.py` + 「推送更新.bat」带重试推送。

**下一步可以做**（没做的都在「五、待办」里）：
- 虚轴之庭详情页那两张模板（`出击`、`快捷战斗确定`）没实测过（需要用户把游戏停到那页抓图）
- 「启动崩坏学园2」的**冷启动**没完整跑过
- BONUS 多轮（`次数=2`）与「体力不足 → 免费双倍体力」那条链没在真机重跑
- 「使魔优先派遣」还是空壳（缺 S/A/B/C 徽章模板；界面上使魔全在探险中时截不到）

---

## 九、关键文件

| 文件 | 说明 |
| --- | --- |
| `assets/interface.json` | 任务与选项定义（**JSONC，带注释，不能用 json.loads 直接读**） |
| `assets/resource/pipeline/30_清日常.json` | 主流程，**143 个节点** |
| `assets/resource/pipeline/00_通用.json` | 通用节点（主界面判定、退一步等） |
| `agent/my_bonus.py` | `bonus_battle`：刷 N 轮 + 边走边打 + 认结算 + 体力不足处理 |
| `agent/battle_config.json` | 所有战斗坐标（`bonus_battle` 段带注释） |
| `agent/main.py` | agent 入口，socket_id 解析 + `start_up` 返回值检查 |
| `tools/run_task.py` | **命令行跑任务的替代方案**（GUI 用不了时靠它）；agent 的输出会落到 `debug/agent.log` |
| `tools/crop.py` | 把截图裁一块放大看（带坐标网格），**标坐标用它，别眯眼看全屏预览图** |
| `tools/burst.py` | 连拍模拟器画面（跑任务时后台开着，事后翻帧看是在哪一屏跑偏的） |
| `tools/dump_nodes.py` | 打印「应用任务默认选项之后」的节点真实内容（看 override 到底生效成什么样） |
| `tools/check_fallback.py` | 扫「next 里没有必然命中兜底节点」的节点（坑 4 / 33 的体检工具） |
| `tools/check_option_conflict.py` | 扫「同一节点同一字段被两处写」的选项冲突（坑 34 / 35 的体检工具） |
| `tools/templates.json` | 模板图清单（57 张，带说明和实测分数） |
| `README.md` | 完整项目文档 |

---

## 十、给下一轮的建议

1. **先跑 `python tools/run_task.py`** 确认工具还能用，再看任务列表。
2. **第一件该做的事**：确认体力 ≥ 50（BONUS 一轮 50），然后跑
   `python tools/run_task.py 重复刷关 --option 次数=1`，看 `开战` 这次认不认得到
   （详见第 5 节待办 1）。**开跑前先 `python tools/burst.py` 挂个连拍**，
   万一又卡住，翻帧比猜快一百倍。跑完看 `debug/agent.log` 里的 `[bonus]` 日志。
   ⚠️ 体力不够时任务会**静默结束**（只到 `收尾`），别误判成代码坏了。
3. **量模板分数比调坐标有用**：`python tools/check_template.py <模板> --shots <一整目录截图>`
   能一眼看出"真命中"和"最像的错图"差多少。这轮就是靠它把两个阈值定下来的。
4. 改完 pipeline **记得 `python tools/get_gui.py --sync-only`**，并跑一次
   `python tools/check_env.py` + `python tools/validate_schema.py`。
5. **改 `interface.json` 时小心**：它是 JSONC，用正则/文本替换时容易吃掉结构
   （我踩过：吃掉 task 数组的闭合括号、留下重复键、删掉整个选项定义）。
   改完务必用 `object_pairs_hook` 查一遍重复键，并跑 validate_schema。
6. **别在没体力的时候试 BONUS** —— 体力 < 50 时底部按钮是「补充体力」，
   点了会弹花水晶的窗，容易误点。要测就先把体力攒够。

48. **「必然命中」的节点会把整条链锁死在它自己的循环里**（启动游戏就是这个病）——
    `启动游戏`（StartApp）的 next 是 `[资源下载提示, 标题页, 等主界面]`，而
    `启动_等主界面` 是个 **DoNothing（必然命中）**，它自己的 next 又只有自己 + 门卫：
    ```
    启动_等主界面.next = [通用_等网络连接, 主界面_就绪, 启动_等主界面]
    ```
    于是：游戏刚启动、标题页还没出来那一下「标题页」认不到 → 流程掉进「等主界面」**自己循环**
    → **再也不会回头看标题页** → 标题页后来出来了也没人点「账号切换」→ 等满 max_hit 后任务失败。
    实测现象：日志里全是「等主界面」、一次都没轮到「标题页」；而同一屏用模板量是 **0.9834**，
    模板根本没坏 —— 坏的是**调度**。
    **修法**：让「等主界面」每轮都回头检查一遍（它是等待节点，本来就该干这个）：
    ```
    启动_等主界面.next = [通用_等网络连接, 启动_标题页, 启动_资源下载提示, 主界面_就绪, 启动_等主界面]
    ```
    同时给 `启动_资源下载提示` 补了出路（原来 next 只有自己，那个提示一消失就必然 20 秒超时）。
    修完实测冷启动：`StartApp → 等主界面×10 → 标题页 命中 → 等主界面 → 主界面_就绪`，
    `succeeded=True` ✅；游戏已在前台时再跑，3 个节点就结束、**不会重启游戏** ✅。
    **通用教训：等待类节点（必然命中的 DoNothing）的 next 里，必须带上"这一轮要检查的所有状态"，
    否则它会把自己变成黑洞。**

49. **BONUS 关入口的偏移一直是错的 —— 所以「关卡战斗被跳过了」（2026-09-16 定位）**。
    `日常_活动BONUS_点左上` / `点左上2` 是两个 `TemplateMatch 日常/活动_BONUS.png`
    + `target_offset: [-4, 78]` 的节点。按 MAA 的算法，落点是
    `(176+58-4, 240+20+78) = (230,338)` —— **实测是点空**，什么都没发生。
    而点空是**静默**的：链子不会报错，只会安静地掉到 `收尾`，外面看起来就是
    「关卡战斗被跳过 / 任务秒完成」。
    **正确的落点是 (254,298)**（偏移 `[20, 38]`），四种方式实测都对得上：
    - 用 `getevent` 录到用户手动点进去的那一下，原始坐标换算成屏幕坐标 = (254,298)
    - 我在这一页网格扫了 112 个点，只有它进得去
    - 改成偏移 `[20,38]` 后跑「回列表→重进活动→认标签→点」循环，**4/4 全部进详情页**
    - 独立跑 pipeline 节点也确认
    ⚠️ 但**别把它当成"固定坐标"来理解**（用户特意提醒过）：BONUS 关的位置会变，
    正确姿势是**先认金色「BONUS」标签，再按相对偏移点** —— 现在实现成了
    agent 的自定义动作 `bonus_enter`（见坑 50）。

50. **BONUS 关入口改成 agent 自定义动作 `bonus_enter`（认内容 + 每步验证）**。
    为什么不能只改偏移：偏移是死的，界面一变（活动轮换、节点挪位、页面没加载完）又会点空，
    而且**点空不会报错**。现在的 `bonus_enter` 做这些事：
    ```
    已在 BONUS 详情页（认「BONUS 标题」+「选择助战好友」）→ 直接过
    在活动地图页（认金色 BONUS 标签）→ 按候选偏移点 → 用详情页标题验证
    在活动列表页（认「难度指数」「剩余时间」）→ 点活动卡片 → 再找标签
    都不认识 → 点「首页」+「战斗」导航（幂等）→ 再看一遍
    全都不行 → 存 debug/bonus_entry_fail_*.png 留证据
    ```
    `日常_活动BONUS_点左上` / `点左上2` 都换成了它；老的
    `日常_活动BONUS`（认 `通用/主界面.png` 当门槛再点 (785,60)）也换掉了 ——
    那个门槛正是坑 51 的雷。

51. **`通用/主界面.png` 在主界面上会掉到 0.7265（阈值 0.75）→ 门槛不命中，模块整条链被跳过**。
    这就是用户报的「使魔探险没做完就结束了」的真身（`日常_使魔探险_开面板` 等 8 个节点
    都拿它当"我在主界面吗"的门槛）。
    原因：这张模板裁的是**侧边栏第一个图标**，而侧边栏是**半透明**的，背后是**会动的**
    主界面插画 —— 背景一变分数就抖。实测（都取 roi `[0,100,240,500]` 内的最高分）：
    | 页面 | 分数 |
    | --- | --- |
    | 主界面（三张不同时刻） | **0.7265** / 0.9118 / 0.8787 |
    | 活动子页 | 0.8896 |
    | 活动列表（战斗→活动） | 0.5170 |
    | BONUS 详情页 | 0.5186 / 0.5414 / 0.5496 |
    | 装备页 | 0.4421 |
    结论：**0.75 卡在主界面自己的抖动区间里**。已把 10 处阈值统一降到 **0.62**
    （离最高假阳性 0.55 还有 0.07 余量，离主界面最低分 0.7265 有 0.10 余量）。
    ⚠️ 这只是止血 —— 真正的解法是**不要拿这张图当门槛**（第 5 节待办里记了）。

52. **`_next_round` 判「到没到好友列表」用错了模板**（同一个 bug 的第二段）。
    原来写的是"认得到 `日常/选择助战好友.png` 吗"——可那个按钮是**详情页**上的，
    **好友列表上根本没有它**。于是：结算完点「再次挑战」、明明已经进了好友列表，
    却被判成"没进好友列表"，直接收工（只刷了 1 轮就不再刷了）。
    证据：它存下来的现场图上 `日常/助战_备用装备.png` 是 **1.0000**（就是好友列表）。
    现在的判据是「**既不在详情页、也不在结算页** → 那就是好友列表」，
    并额外用 `助战_备用装备.png` 做正面确认。

53. **这个 agent 是"瞎"的 —— 直到这轮才有眼睛**。
    `read_image` 在这个模型上直接报「不支持图片输入」，所以之前排查只能靠模板反查猜。
    现在有两个新工具：
    - **`tools/read_screen.py`（OCR，强烈推荐第一个用）**：`python -m pip install rapidocr-onnxruntime`
      装完就能 `python tools/read_screen.py --live` 把屏幕上的字连坐标一起打出来，
      `--tap X Y` 还能"点一下再看"，`--crop x,y,w,h` 只看一块（按钮小字要放大才认得准）。
      这轮全靠它才搞清楚「休诊时间活动页 / 体力 270/205 / 双倍券 66 张 / 详情页布局」。
      **它只是排查工具，pipeline 不依赖它。**
    - **`tools/ascii_view.py`**：截图转字符画（`--color` 按色相），读不出字时看色块布局用。

54. **模拟器的触摸板是「竖屏原始坐标」，换算公式已实测确认**：
    ```
    屏幕x = 原始Y        （原始Y ∈ [0,1280]）
    屏幕y = 720 − 原始X  （原始X ∈ [0,720]）
    ```
    怎么录：`adb shell "timeout 100 getevent -lt /dev/input/event4"`
    （`Xiaomi Input` 这个设备就是触摸板；`adb shell input tap` 注入的事件**不会**出现在这里，
    所以录到的都是用户真实的手指）。
    怎么验：`sendevent /dev/input/event4 ...` **能用**（shell 用户在 `input` 组，
    设备是 `crw-rw---- root:input`）—— 我拿它注入 `(652,860)` 换算成屏幕 (860,68)，
    果然切到了「装备」页，公式就钉死了。
    验证细节：`sendevent $dev 3 57 1; sendevent $dev 1 330 1; sendevent $dev 3 53 X;
    sendevent $dev 3 54 Y; sendevent $dev 0 0 0; sleep 0.08;
    sendevent $dev 3 57 4294967295; sendevent $dev 1 330 0; sendevent $dev 0 0 0`

55. **`tools/template_health.py` 从写出来那天起就是崩的** ——
    `rel_check = os.path.relpath(path, ...)` 用在了 `path = ...` 赋值**之前**，
    一跑就是 `UnboundLocalError: cannot access local variable 'path'`。
    难怪没人用它。（已修；修完顺手验证 `通用/网络连接中.png` 是健康的：
    在连拍帧上 **1.0000 @(428,302)**，正好落在 pipeline 的 roi `[400,280,480,200]` 里。）

56. **`assets/resource/pipeline/40_刷关.json` 是死代码，已删**。
    「重复刷关」早就改走 `30_清日常.json` 的活动 BONUS 链（entry = `日常_重复刷关_入口`），
    那 10 个 `刷关_*` 节点没有任何地方引用，而且里面还挂着两个**不存在的节点名**
    （`回到主界面` / `回到主界面_退一步` —— 其实在 `00_通用.json` 里有，但
    `tools/check_fallback.py` 是按文件查的，一直报"这些节点不存在"）。
    删掉之后 `check_env.py` / `check_fallback.py` 都干净了。

57. **`cv2.imwrite` 在中文用户名路径下会静默失败** ——
    这台机器的临时目录是 `C:\Users\丐帮帮主\AppData\Local\Temp`，`cv2.imwrite` 直接写不进去
    （返回 False 不抛异常）。写法要改成 `cv2.imencode(".png", img)[1].tofile(path)`。
    项目里其他工具早就是这么绕的，新写工具时别再踩。

58. **BONUS 详情页的布局（OCR 实测，2026-09-16）** ——
    | 位置 | 文字 | 模板 |
    | --- | --- | --- |
    | (612,120) 124x40 | BONUS（标题） | `日常/BONUS详情标题.png`（新增，1.0000） |
    | (623,175) | 最高得分：50 | |
    | (622,228) | 可能获得 | |
    | (621,531) | 消耗体力50 | |
    | (852,533) | 使用双倍券（勾选框在它右边 +196） | `日常/使用双倍券.png` 1.0000 |
    | (1016,533) | 66 x（双倍券数量） | |
    | (655,635) | **选择助战好友**（进本按钮） | `日常/选择助战好友.png` 1.0000 @(532,628) |
    ⚠️ 地图页上那个金色「BONUS」标签和详情页这个「BONUS」标题**不是同一张图**：
    金色标签在详情页上只有 **0.456** 分，所以详情页必须用新模板 `日常/BONUS详情标题.png`
    来认（它在详情页 1.0000、活动地图页 0.4764、装备页 0.4500）。
    这个新模板的另一个用处是**防止点歪进错关** —— 别的关卡详情页也有
    「选择助战好友」「使用双倍券」，只有它能证明"我进的是 BONUS 关"。

59. **`agent/my_bonus.py` 的 `_fight` 现在会处理网络异常**（用户报的「进入战斗会因为网络异常卡住」）：
    - 每 `check_every`(3s) 看一次屏幕，优先认**战斗结算**
    - 认到 `通用/网络连接中.png`（实测 1.0000 @(428,302)）就**停下不点**，等它自己好，
      等待的秒数从时间上限里补回来（不算进 `duration`）
    - 「人还在助战页」= 开战按钮还认得到（`日常/开战.png`，**连续两次命中才算**，
      免得把"开战→loading"那一瞬误判）。确认还在就重新点开战，最多 `reentry_limit`(3) 次，
      用完直接收工，不陪它耗满 150 秒
    - 实在打不出结算，把现场存成 `debug/bonus_fail_*.png`（`_dump`）
    `_next_round` / `_finish` 里也各加了一次 `_wait_net`。

60. **别在"没加载完"的页面上点按钮**（同一个坑的第三次）——
    同一段 pipeline（`选好友 → 点好友 → 助战就绪 → 开战`）我跑了三遍：
    - 12:01 那次：全通
    - 13:0x 两次：`选好友` 命中并点了，**但页面纹丝不动**（连拍 80 帧全是详情页），
      后面 `点好友` 当然也认不到 → 一路掉到 `收尾`
    - 13:26 那次：全通（`认到战斗结算（点了 90 次）`）
    期间我用 `adb shell input tap 682 651`（同一个点）手点，**两次都一次就通**，
    独立跑 pipeline 节点也通 —— 所以**不是坐标问题，是"那一刻游戏不吃这一下"**
    （详情页/好友列表都还在加载，网络一慢窗口就拉长）。
    **教训：凡是"点完要换页"的步骤，都要能验证 + 重试**，别信"命中即成功"。
    目前 `bonus_enter`（找入口）已经是这个写法；`选好友` 那一步还没加，
    下次再遇到就照着 `bonus_enter` 的样子补一个 `bonus_pick_support`。
    **已经做的兜底**：给 7 个"点导航/侧边栏 → 再认内容"的模块节点加了**自重试**
    （把节点自己插进 `next` 的内容检查之后、兜底之前，`max_hit` 提到 3）——
    `python debug/add_retry.py --write` 就是干这个的。这样第一次点不动，
    下一轮会先看"面板开没开"，没开就再点一次。
    ⚠️ 只能给**幂等**的点击加（侧边栏图标、导航栏），会弹窗/会切换状态的按钮不能这么干。

61. **「使魔探险没做完就结束了」有一部分是"本来就没事干"**（2026-09-16 实测）——
    任务面板 OCR 出来是：
    ```
    使魔探险将于每日4:00刷新
    ·已接取的探险数3/10
    水晶的微光  剩余时间：08:21:32
    酒杯转转转  剩余时间：06:21:41
    双倍的快乐  剩余时间：06:21:41   ← 三条都在探险中
    ```
    而 `日常/一键派遣.png` 只有 **0.4403**、`日常/出发.png` 只有 **0.3469**
    —— 也就是**面板上根本没有这两个按钮**（没得派的时候它们就不画）。
    所以链子走到 `翻页 → 收尾` 收场是**对的**，不是 bug。
    顺带把面板上的模板分数也量了（都在同一张 `debug/sm1.png` 上）：
    `任务_使魔探险_选中.png` **1.0000** @(28,362) ／ `任务_使魔探险.png`（未选中）0.6808
    ／ `任务_存在感_未选.png` 0.9916 ／ `任务_每周任务.png` 0.9997。
    **判断"该不该派遣"要看按钮在不在，别拿"任务花了多少秒"当判据。**

62. **`tools/get_gui.py --sync-only` 会把 GUI 里的版本号打回 0.1.0**（2026-09-16 发现并修）——
    `sync_assets()` 的签名是 `def sync_assets(version="0.1.0")`，完整流程那一路会
    `sync_assets(version=read_version())`，但 `--sync-only` 那一路**没传 version**，
    于是每次同步都把 `gui/interface.json` 的 `version` 改写回写死的 `0.1.0`。
    发版之后同步一下就"版本倒退"，而界面上的版本号很难被注意到。
    已改成 `sync_assets(version=read_version())`。**踩法验证**：同步完
    `Select-String gui\interface.json -Pattern '"version"'` 看一眼是不是你要的那个版本。

63. **「虚轴之庭」和「使魔的爱」这两条链从来就没通过**（2026-09-16 实测修正）——
    用户报的「还没打就退出了」，原因是链子里有一个认 `日常/出击.png` 的节点：
    **那张模板在真页面上只有 0.3781 ~ 0.7470 分，一次都没命中过**，而且整页 OCR
    都读不到「出击」两个字 —— 基本可以确定是废图（大概裁自某个不存在/改版前的界面）。
    点位认不到 → 直接掉 `收尾` → 外面看就是"还没打就退出"。
    **实测出来的真实路径**（每一张都在真机帧上量过）：
    ```
    战斗 nav → [活动列表]   多元裂缝 卡片 @(752,169)        0.9999
             → [多元裂缝列表] 虚轴之庭 卡片 @(836,232)        1.0000
                             使魔的爱 卡片 @(593,440,115,105)  1.0000   ← 注意是"卡片"不是标题
             → [虚轴之庭地图页] 关卡入口 @(657,492)           （新增节点「点关卡」）
             → [关卡详情页]   快捷战斗 @(900,628)            0.9998
                             选择助战好友 @(532,628)         0.9971
             → [快捷战斗弹窗] 确定 @(488,448) = 快捷战斗确定.png 1.0000（取消在右边 (693,455)）
             → [结算页]      确定 @(716,588) = 结算确定.png   1.0000
    ```
    两个新模板（都做过判别力实测）：
    | 模板 | 尺寸 | 该页 | 别的页最高 |
    | --- | --- | --- | --- |
    | `日常/使魔的爱_卡片.png` | 115x105 | 活动列表 1.0000 | 0.46（关卡详情页） |
    | `日常/虚轴之庭_关卡入口.png` | 60x60 | 虚轴之庭地图页 1.0000（多帧同分，那页是静态的） | 0.56（主界面） |
    ⚠️ **使魔的爱 不能按标题文字点**：`使魔的爱_文字.png`（129x30 标题）在列表上是 0.9980，
    但**点它没有任何反应**（标题周围我试了 6 个点全都没反应）—— 要点的是**卡片本体**
    （(593,440)-(708,545)）。虚轴之庭同理：列表上点卡片文字进的是**地图页**，
    还要再点一次地图上的关卡入口才到详情页。
    **实测结果**：虚轴之庭连刷 2 次 ✓、使魔的爱刷 1 次 ✓（都是「快捷战斗」→「确定」→「结算确定」）。

64. **「快捷战斗」是免费扫荡，不花体力** —— 关卡详情页右下角写着「今日免费 6/6」
    （每扫一次扣 2），另有「今日剩余次数 N/M」是关卡自己的次数限制。
    所以测试这两条链不会烧体力，但会吃掉当天的免费次数。

65. **`next` 里"无识别的纯点击"兜底会抢在识别节点前面**（和坑 25 同源，这次踩在模块入口上）——
    `日常_虚轴之庭.next = [进多元, 回列表, 翻页, 开列表(纯 Click，必然命中), ...]`：
    MAA 每轮按顺序试候选，`进多元` 需要等页面加载出来才认得到，而 `开列表` 第一轮就命中，
    于是**每轮都被它吃掉**。修法是给识别节点加**自重试**（把自己放进 `next` 并 `max_hit`），
    别让兜底抢跑。
    实测还发现：`开列表`（点右上角活动名）是"**往上一层**"，而这两个模块要往上 **1~2 层**
    才能到活动列表 —— 所以给它自己加了自重试（最多点 3 次，每次 3 秒后再看一遍），
    表现就是：一次点不动/层数不够，会自己再往上点一层，最后才 `逃逸`。
    改完这次两个模块**连着跑**也顺了：`虚轴之庭 开列表→进多元→点卡→点关卡` ，
    `使魔的爱 开列表→点卡（卡片）` ✓（这次两边当日次数都用完了，所以没扫荡，这是对的）。

---

## 十一、2026-09-16 下午这一轮的实测记录（BONUS 链终于跑通）

跑法：`python tools/run_task.py 关卡战斗`（体力 220，BONUS 一轮 50）。
跑通那次的节点链（`debug/t9.log`）：
```
关卡战斗_入口 → 关卡战斗_就绪 → 日常_开始 → 步骤2..6 → 日常_活动BONUS（自定义动作，进详情页）
→ 选好友 → 点好友（助战_备用装备 命中）→ 助战就绪 → 开战 → 打完_自动 → 收尾 → 步骤7 → 收尾
```
agent 日志（`debug/agent.log`）：
```
[bonus] 认不出这是哪一页 —— 点「首页」再点「战斗」（幂等操作）
[bonus] 导航一圈之后认到 BONUS 标签 @(176,240,116,40)，中心 (234,260)
[bonus]   点「BONUS 入口候选1（偏移 [20, 38]）」(254, 298)
[bonus] 进 BONUS 详情页了（偏移 [20, 38] 生效）
[bonus] 开刷：999 轮，拖动输出，水晶补体力 关
[bonus] === 第 1/999 轮 ===
[bonus]   认到战斗结算（点了 91 次）
[bonus]   认到「再次挑战」（体力够）→ 点「再次挑战」→ 到好友列表了（认到「备用装备」那一格）
[bonus]   点「选备用装备」(464, 255) → 点「开战」[879, 659]
[bonus] === 第 2/999 轮 ===    …（同样跑通）
```
**能下结论的**：入口、选好友、开战、`认结算`、`再次挑战 → 下一轮` 这一整条**通了**，
而且 `日常/战斗结算.png` 这个模板是**好的**（以前"150 秒认不到结算"，真相是**战斗根本没开起来**）。

**还没验的**：
- 体力耗尽那一刻的表现（`补充体力` 弹窗 → 点取消 → 收尾）—— 这轮刚好没跑到
- 「使用双倍券」那条路（详情页上显示有 66 张，勾选框在 `使用双倍券.png` 右边 +196）
- 虚轴之庭 / 使魔的爱那两条链（它们走的是 `日常_开战 → 自动战斗_打到结束`，另一个 action）

**这一轮改动的文件**（都跑过 `validate_schema` + `check_env`）：

| 文件 | 改了什么 |
| --- | --- |
| `agent/my_bonus.py` | 新增 `bonus_enter`（认内容找入口，旧名 `bonus_find_entry` 保留兼容）；`_fight` 加网络等待 / 开战重试 / 失败存图；`_next_round` 修「好友列表」判据、加 `_wait_net`；`_Ctl` 加 `save_shot` |
| `assets/resource/pipeline/30_清日常.json` | `日常_活动BONUS` / `点左上` / `点左上2` 换成自定义动作；8 处 `通用/主界面.png` 阈值 0.75 → 0.62 |
| `assets/resource/pipeline/00_通用.json` | `主界面_就绪` / `回到主界面` 阈值 0.75 → 0.62 |
| `assets/resource/image/日常/BONUS详情标题.png` | 新增模板（详情页 BONUS 标题，实测 1.0000） |
| `assets/resource/pipeline/40_刷关.json` | **删除**（死代码） |
| `tools/read_screen.py` | 新增：OCR 读屏（排查用，pipeline 不依赖） |
| `tools/ascii_view.py` | 新增：截图转字符画 |
| `tools/template_health.py` | 修 `UnboundLocalError` |
| `tools/templates.json` | 更新 `活动_BONUS` 说明、登记新模板 |
| `agent/battle_config.json` | 新增网络等待 / 入口候选偏移 / 导航点击点等参数 |