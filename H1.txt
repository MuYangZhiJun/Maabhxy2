##### HANDOFF 共 1070 行，命中 71 段 #####

  103| 使魔探险        → 使魔优先派遣
  104| 商店购物        → （无选项）
  105| 关卡战斗        → 日常模块 / 自动模式 / 使用双倍卷
  106| 领取奖励        → 领取奖励项目
  107| 领取邮件        → 邮件操作

  109| ```
  110| 
  111| **「关卡战斗」的「打哪些」只剩**：虚轴之庭 / 使魔的爱 / 活动 BONUS（默认只勾 BONUS）
  112| 
  113| ---

  121| | 使魔探险 | ✅ 端到端跑通 | 领取奖励 + 一键派遣 |
  122| | 商店购物 | ⚠️ 链路跑通，但买的是**零时馈礼**不是零时之种 | 买完就下架 |
  123| | 虚轴之庭 | ✅ 端到端跑通 | 战斗→活动→多元裂缝→虚轴之庭→出击→扫荡 |
  124| | 使魔的爱 | ✅ 链路通 | 同路径，用「快捷战斗」扫荡 |
  125| | 活动 BONUS | ✅ **导航链 + 进本 + 打完 + 结算全部实测跑通**；体力不足那步刚修完没重跑 | 见待办 1 |

  130| ## 五、待办（按优先级）
  131| 
  132| 1. **BONUS 已经跑通了，只剩「体力不足 → 继续下一轮」那条链没在真机上重跑** ——
  133| 
  134|    实测那一轮（`python tools/run_task.py 重复刷关 --option 次数=2`，体力 50）的节点日志：

  135| 
  136|    ```
  137|    日常_活动BONUS             命中 Rect(18,12,245,45)          主界面
  138|    日常_活动BONUS_开列表        命中                            点右上角活动名回列表（战斗导航落在子页了）
  139|    日常_活动BONUS_进活动        命中 Rect(436,168,116,36)      活动列表里的「难度指数：B」

  140|    日常_活动BONUS_点左上        命中                            地图第 2/2 页左上角 = BONUS 关
  141|    日常_活动BONUS_选好友        命中 Rect(532,628,300,46)      1.0000，阈值 0.95 卡得很干净
  142|    日常_活动BONUS_点好友        命中
  143|    日常_活动BONUS_开战         命中 Rect(778,632,202,54)      ← 上一轮就是这一步掉链子的
  144|    日常_活动BONUS_打完_自动     命中                            agent 的 bonus_battle 跑起来了

  148|    连拍帧复核：结算页在**开跑后 64.5 秒**出现（战斗本身约 14 秒），
  149|    `战斗结算.png` 稳定 1.0000 @ (566,112)，和 agent 的 `settle_roi [400,80,600,90]` 对得上。
  150|    **确认了 BONUS 关是自动战斗**：放手不管 11 秒自己打完（挑战时间 11、受击 0、最大连击 27、积分 50、S 级）。
  151| 
  152|    **没跑完的一截**：体力只有 50，打完第 1 轮就剩 0；agent 想去刷第 2 轮，

  165|    （大概要号上有双倍体力可换）。
  166| 
  167|    **体力恢复实测 ≈ 7 分钟 1 点**（48→49→50 花了约 13 分钟），一轮 BONUS 要 50 点 ——
  168|    也就是说这个功能的验证成本挺高的，一次别浪费。
  169| 

  176|    | 结算页「战斗结算」标题 | 命中 (566,112) | `settle_roi [400,80,600,90]` 装得下 ✅ |
  177|    | 好友列表第一格 | 实际 (234,182)-(694,329) | `support_slot [462,264]` **在格子里** ✅ |
  178|    | 「开战」按钮 | 模板 202x54 命中 (778,632)，1.0000 | 在 `roi [600,580,600,140]` 里 ✅ |
  179|    | 「选择助战好友」按钮 | 模板 300x46 命中 (532,628)，1.0000 | 点它正中进好友列表 ✅ |
  180|    | 「补充体力」窗 | 标题命中 (574,229)、购买中心 (533,456)、取消中心 (749,456) | 买 `[-111,200]` ✅ / 取消 `[109,199]` ✅ |

  186|    每轮「关 N」都有返回箭头，链路是自洽的；本轮开工时游戏卡在邮件详情页，是上一轮手动测试的残留。
  187| 
  188| 3. **体力不够时任务是静默结束的** —— 体力 < 50（一轮要 50）时 BONUS 详情页的按钮会从
  189|    「选择助战好友」变成「补充体力」，`选好友` 认不到、`翻页` 也被门槛图挡住了，
  190|    于是整条链直接掉到 `收尾` 回主界面：**日志上看不出来是"没体力"，只像是没干活**。

  193| 
  194| 4. **「使用双倍卷」开关以前是坏的，修了但没实测** —— `interface.json` 里「重复刷关」的任务级
  195|    `pipeline_override` 强行把 `日常_活动BONUS_双倍券` 打开了，而末位合并会把选项的 No 覆盖掉，
  196|    等于勾不勾都跑双倍券。已经把那三行删掉。同时 `target_offset` 从 `[123,0,0,0]` 改成 `[196,0,0,0]`
  197|    —— 复选框真实位置是 (1085,527)-(1113,560)、中心 (1099,543)，原来的 123 偏到了文字右边的空白处。

  214|    override 是末位覆盖：**后合并的那个没法知道前一个选了什么**，所以拼不出全部四种组合。
  215|    原来的写法是「自动模式=Yes」顺手把 `打完_自动_水晶` 也打开了，导致**默认设置
  216|    （碎水晶=否）下 `打完_自动_水晶` 和 `打完_自动` 同时 enabled**，而 `开战` 的 next 里
  217|    水晶版排在前面 → **没勾碎水晶也会花水晶**。已修的版本：`自动模式` 只开自己那一个不带水晶的，
  218|    `碎水晶=No` 显式把两个水晶版关掉 —— 现在四种组合里三种正确，

  224| ## 五之二、用户 2026-09-16 报的 4 个问题（正在处理）
  225| 
  226| 用户原话：「领取邮箱显示任务错误，关卡战斗有概率点歪，使魔之爱没有正常开始
  227| （使用图标标记，但图标会每日轮替），"存在感"有时候不会主动切换去领取」。
  228| 

  278| - 「自动模式 × 碎水晶」的四组合问题（见待办 9）。
  279| 
  280| ### ③ 关卡战斗「有概率点歪」　✅ **已改（BONUS 那条链）**
  281| 
  282| `日常_活动BONUS_点左上` / `点左上2` 原来是**死坐标点击 `(245,325)`**（上次只给 `翻页`

  283| 加了门槛图，漏了这两个）。活动一换、BONUS 节点在地图上的位置一变就点歪。
  284| 
  285| 已改成用**金色 BONUS 标签定位**：`TemplateMatch 日常/活动_BONUS.png`，
  286| 实测地图页 **1.0000 @ (176,240)**，关卡卡片就在标签正下方 → `target_offset: [-4, 78, 0, 0]`，
  287| 阈值 0.95。这样跟页面在第几页、活动怎么轮替都无关。

  289| （那一页上 `选择助战好友.png` 1.0000、`使用双倍券.png` 1.0000）✅
  290| 
  291| ### ④ 使魔的爱没正常开始　✅ **已改＋已现场验证**
  292| 
  293| **（我先前的判断有一半是错的，这里更正）**：

  295| **其实模板没问题** —— 问题在**活动列表的滚动位置**：
  296| 
  297| | 列表状态 | 多元裂缝 卡片 | `多元裂缝.png` |
  298| | --- | --- | --- |
  299| | 滚到能看见卡片 | 完整 | **0.9997 @ (752,340)** ✅ |

  304| 所以这一步时灵时不灵 —— 这就是"没正常开始"和"有概率点歪"的观感来源。
  305| 
  306| **已做的三件事**（`使魔的爱` / `虚轴之庭` 两条链都改了）：
  307| 1. 入口 next 接上已经在 pipeline 里的 `_翻页`（就是那个 Swipe 滚动）——
  308|    它以前只能从 `_回列表` 到达，而 `_回列表` 认的是关卡详情页的按钮，入口根本到不了。

  311|    必然命中，兜到底再去做下一个模块）—— 补上「next 全不命中就死等 20 秒」这个老坑。
  312| 3. **点卡改用标签文字**（用户的提示：「使用图标标记，但图标会每日轮替」）——
  313|    新裁 `日常/使魔的爱_文字.png`（129x30），`target_offset: [-9, 88, 0, 0]` 偏到图标卡上。
  314| 
  315| **现场验证过**：标签模板在聚合页 0.9980 @ (587,394)，按偏移点下去 → 关卡详情页开出来

  320| `_开列表` 就是这么排的，别把它提到前面去。
  321| 
  322| ⚠️ **还没验的**：把 `关卡战斗` 的「日常模块」只勾 `使魔的爱` 跑一次完整链路
  323| （会花体力扫荡一次，也正好推进用户那条每日任务「通过一次多元裂缝关卡『使魔的爱』」）。
  324| 

  332|    所以「模块跑完回调度器、靠 max_hit 跳过」的写法会死循环。
  333|    现在用的是**单向链式调度**：`日常_开始 → 日常_步骤2 → … → 日常_步骤7`。
  334|    「刷 N 轮」也不能靠它，得在 agent 里自己数（BONUS 就是这么做的）。
  335| 
  336| 2. **`custom_action_param` 是整体替换，不是深合并** —— 官方文档原话"直接替换，内部不会合并"。

  389|     在任务面板里也命中 0.97。改用「返回箭头在不在」（`inverse: true`）。
  390| 
  391| 16. **活动子页的返回方式：点右上角那个活动名字** —— 虚轴之庭、崩坏之塔这类全屏子页
  392|     **没有返回箭头**，导航栏也点不动。
  393|     ⚠️ 按系统返回键是弹**「确认退出游戏」**，别按。

  400| 19. **这类刷刷刷活动的关卡入口就在活动页左上角**，固定坐标点过去就行，不用每次截图。
  401| 
  402| 20. **「快捷战斗」是扫荡不是进战斗** —— 点了弹数量窗（默认 2 次，另有 Max），
  403|     确定后直接出结算。「选择助战好友」才是手动进本。
  404| 

  414|     两个窗按钮位置一样但标题宽度不同，`target_offset` 要各算各的：
  415|     兑换 `[-168, 199]`、买体力 `[-111, 200]`。
  416|     ⚠️ **补充（2026-09 实测）**：BONUS 结算页上体力为 0 时点「再次挑战」，
  417|     弹出来的是**「补充体力」（花 60 水晶）**，**没有**免费那条路。
  418|     所以别默认「体力不足 = 免费双倍体力」——「兑换双倍体力」什么时候才出现还没搞清楚

  423|     也就是说候选节点本来会被反复重试最多 20 秒。**但只要列表里有个纯 Click 的兜底节点
  424|     （比如 `收尾`），第一轮就命中，后面所有候选就再也不试了。**
  425|     踩到的例子：`点好友 → [开战, 收尾]`，`开战` 那一瞬间没认出好友面板（怀疑面板还在滑），
  426|     立刻掉到 `收尾` 回主界面，一轮白跑。**排查方向是「给动作加 `post_wait_freezes`
  427|     等画面稳定」，而不是「把坐标再调准一点」。**

  430| 
  431| 26. **同一个坐标在不同界面上可能是两个完全不同的东西** ——
  432|     地图页的 (735,655) 是「下一页 ▶」，可 BONUS 关详情页的同一个点是「选择助战好友」。
  433|     `翻页` 原本是纯按坐标点，结果在详情页上等于点了进本按钮，**直接把战斗开起来了**。
  434|     修法：给动作加**门槛图** —— 先把 ▶ 裁成模板（`日常/活动_翻页.png`），

  436|     都要问一句：这个坐标在别的界面上会是什么。**
  437| 
  438| 27. **按钮文案会变，模板就会假阳性** —— BONUS 详情页底部那个按钮，体力够时是
  439|     「选择助战好友」，体力不够时变成「补充体力」。而 `选择助战好友.png` 在**错的那屏**
  440|     上照样有 **0.9376** 分（默认阈值 0.85 → 认了），点下去白弹一个花水晶的窗。

  448|     才会出现。所以自动流程里"体力不够"必须靠"能不能认到按钮"来判断，不能靠"点了看看弹什么"。
  449| 
  450| 29. **活动关地图的 BONUS 关在第 2/2 页的左上角，第 1/1 页那个位置是空的** ——
  451|     所以 `点左上` 那个固定坐标 (245,325) 在第 1 页上是**点空**（安全，什么都不会发生），
  452|     流程靠 `翻页` 翻到第 2 页再点一次。这条链是自洽的，不用怀疑它。

  455| 30. **`战斗` 导航不保证落在活动列表页，再点一次也没用** —— 游戏记住的是**上次停在哪个
  456|     活动子页**（比如「休诊时间」关地图），点「战斗」直接回到那个子页，**连点两次也是原地不动**。
  457|     回列表唯一的办法是点**右上角那个活动名按钮**（休诊时间/多元裂缝…位置一样，在
  458|     `(1080,140,140,50)`）。这就是 `日常_活动BONUS_开列表` 这个节点的用途：它排在 `进活动`
  459|     **后面**当兜底 —— 已经在列表页时 `进活动` 先命中，它就不会执行。

  491|     合并顺序是「先各选项的 case，最后任务级」，所以任务级里写了的字段，
  492|     选项再也改不动。踩到两次：
  493|     - 「重复刷关」强行 `日常_活动BONUS_双倍券.enabled = true`
  494|     - 「领取奖励」把 8 个存在感/每周任务节点全部强行 `enabled = true`
  495|       → **「领取奖励项目」那三个勾选框完全无效**

  496|     **任务级只该写"入口 + 收尾"，其余交给选项。**
  497| 
  498| 36. **模板是会过期的，而且过期是静默的** —— `日常/多元裂缝.png` 现在
  499|     **123 张历史截图里一张都认不到**（最高 0.46）。节点不报错，只是永远不命中，
  500|     整条链安静地掉到收尾（表现为"这个模块没跑"）。

  506|     分数到 0.95 以上才允许用来裁模板。**
  507|     三次实证：
  508|     - 「使魔的爱」标签图 160x49：原样 **0.6017**，缩回 1280 后 0.9871 ✅
  509|     - 「多元裂缝」标题图 248x64：原样 **0.4874**，缩回 1280 后 0.9899 ✅
  510|     - 自己从 1280x720 原生截图裁的：直接 1.0000 ✅

  534|     实测判别力（阈值 **0.75**，中间有 0.25 干净间隔）：
  535|     主界面 1.0000 / 0.9134 / 0.8587；邮件页 0.5533、吼姆页 0.4875、活动列表 0.6001、
  536|     任务面板 0.4742、开战页 0.6136。
  537|     ⚠️ 侧边栏图标是半透明块、底下场景会动，主界面侧分数会抖到 0.86 —— **阈值别写 0.85**。
  538|     ⚠️ **试过但不成**：`And(all_of=[信息板, 返回箭头 inverse])` —— 实测 `And` 里无论是

  562|     （六个任务的 `entry` 都已指向新的 `*_入口` 节点。）
  563| 
  564| 44. **「重复刷关」和「关卡战斗 · 活动 BONUS」是同一套逻辑** ——
  565|     同一条链、同一批节点、同一个 agent 动作（`bonus_battle`），改一处两边都受影响。
  566|     区别只有两个选项：

  614|     两个节点互相出现在对方 next 里、并且自环（一次可能连弹几个）。
  615|     ①式的模板上一轮实测过 1.0000/关闭后 0.3164；②式的模板本轮实测 1.0000
  616|     （在「补充体力」窗上 0.5746、开战页 0.4449，不会误认）。
  617| 
  618| 41. **GUI 实例配置里，只有"勾选"需要手改，override 不用管** ——

  665|    agent 连不上、界面卡「正在启动 Agent」。装了 `maafw==5.13.0` 后通了；
  666|    `agent/main.py` 现在会检查 `start_up()` 返回值，连不上直接报错退出（不再空等）。
  667| 3. **修 BONUS 链 + 补工具**：`tools/crop.py`（裁图带网格）、`tools/burst.py`（连拍翻帧）、
  668|    `tools/dump_nodes.py`（看 override 最终生效值）、`tools/pick_template.py`、
  669|    `tools/template_health.py`、`tools/check_fallback.py`、`tools/check_option_conflict.py`。

  678| 
  679| **下一步可以做**（没做的都在「五、待办」里）：
  680| - 虚轴之庭详情页那两张模板（`出击`、`快捷战斗确定`）没实测过（需要用户把游戏停到那页抓图）
  681| - 「启动崩坏学园2」的**冷启动**没完整跑过
  682| - BONUS 多轮（`次数=2`）与「体力不足 → 免费双倍体力」那条链没在真机重跑

  709| 
  710| 1. **先跑 `python tools/run_task.py`** 确认工具还能用，再看任务列表。
  711| 2. **第一件该做的事**：确认体力 ≥ 50（BONUS 一轮 50），然后跑
  712|    `python tools/run_task.py 重复刷关 --option 次数=1`，看 `开战` 这次认不认得到
  713|    （详见第 5 节待办 1）。**开跑前先 `python tools/burst.py` 挂个连拍**，

  721|    （我踩过：吃掉 task 数组的闭合括号、留下重复键、删掉整个选项定义）。
  722|    改完务必用 `object_pairs_hook` 查一遍重复键，并跑 validate_schema。
  723| 6. **别在没体力的时候试 BONUS** —— 体力 < 50 时底部按钮是「补充体力」，
  724|    点了会弹花水晶的窗，容易误点。要测就先把体力攒够。
  725| 

  744|     否则它会把自己变成黑洞。**
  745| 
  746| 49. **BONUS 关入口的偏移一直是错的 —— 所以「关卡战斗被跳过了」（2026-09-16 定位）**。
  747|     `日常_活动BONUS_点左上` / `点左上2` 是两个 `TemplateMatch 日常/活动_BONUS.png`
  748|     + `target_offset: [-4, 78]` 的节点。按 MAA 的算法，落点是

  749|     `(176+58-4, 240+20+78) = (230,338)` —— **实测是点空**，什么都没发生。
  750|     而点空是**静默**的：链子不会报错，只会安静地掉到 `收尾`，外面看起来就是
  751|     「关卡战斗被跳过 / 任务秒完成」。
  752|     **正确的落点是 (254,298)**（偏移 `[20, 38]`），四种方式实测都对得上：
  753|     - 用 `getevent` 录到用户手动点进去的那一下，原始坐标换算成屏幕坐标 = (254,298)

  755|     - 改成偏移 `[20,38]` 后跑「回列表→重进活动→认标签→点」循环，**4/4 全部进详情页**
  756|     - 独立跑 pipeline 节点也确认
  757|     ⚠️ 但**别把它当成"固定坐标"来理解**（用户特意提醒过）：BONUS 关的位置会变，
  758|     正确姿势是**先认金色「BONUS」标签，再按相对偏移点** —— 现在实现成了
  759|     agent 的自定义动作 `bonus_enter`（见坑 50）。

  763|     而且**点空不会报错**。现在的 `bonus_enter` 做这些事：
  764|     ```
  765|     已在 BONUS 详情页（认「BONUS 标题」+「选择助战好友」）→ 直接过
  766|     在活动地图页（认金色 BONUS 标签）→ 按候选偏移点 → 用详情页标题验证
  767|     在活动列表页（认「难度指数」「剩余时间」）→ 点活动卡片 → 再找标签

  769|     全都不行 → 存 debug/bonus_entry_fail_*.png 留证据
  770|     ```
  771|     `日常_活动BONUS_点左上` / `点左上2` 都换成了它；老的
  772|     `日常_活动BONUS`（认 `通用/主界面.png` 当门槛再点 (785,60)）也换掉了 ——
  773|     那个门槛正是坑 51 的雷。

  783|     | 活动子页 | 0.8896 |
  784|     | 活动列表（战斗→活动） | 0.5170 |
  785|     | BONUS 详情页 | 0.5186 / 0.5414 / 0.5496 |
  786|     | 装备页 | 0.4421 |
  787|     结论：**0.75 卡在主界面自己的抖动区间里**。已把 10 处阈值统一降到 **0.62**

  790| 
  791| 52. **`_next_round` 判「到没到好友列表」用错了模板**（同一个 bug 的第二段）。
  792|     原来写的是"认得到 `日常/选择助战好友.png` 吗"——可那个按钮是**详情页**上的，
  793|     **好友列表上根本没有它**。于是：结算完点「再次挑战」、明明已经进了好友列表，
  794|     却被判成"没进好友列表"，直接收工（只刷了 1 轮就不再刷了）。

  795|     证据：它存下来的现场图上 `日常/助战_备用装备.png` 是 **1.0000**（就是好友列表）。
  796|     现在的判据是「**既不在详情页、也不在结算页** → 那就是好友列表」，
  797|     并额外用 `助战_备用装备.png` 做正面确认。
  798| 
  799| 53. **这个 agent 是"瞎"的 —— 直到这轮才有眼睛**。

  829| 
  830| 56. **`assets/resource/pipeline/40_刷关.json` 是死代码，已删**。
  831|     「重复刷关」早就改走 `30_清日常.json` 的活动 BONUS 链（entry = `日常_重复刷关_入口`），
  832|     那 10 个 `刷关_*` 节点没有任何地方引用，而且里面还挂着两个**不存在的节点名**
  833|     （`回到主界面` / `回到主界面_退一步` —— 其实在 `00_通用.json` 里有，但

  840|     项目里其他工具早就是这么绕的，新写工具时别再踩。
  841| 
  842| 58. **BONUS 详情页的布局（OCR 实测，2026-09-16）** ——
  843|     | 位置 | 文字 | 模板 |
  844|     | --- | --- | --- |

  849|     | (852,533) | 使用双倍券（勾选框在它右边 +196） | `日常/使用双倍券.png` 1.0000 |
  850|     | (1016,533) | 66 x（双倍券数量） | |
  851|     | (655,635) | **选择助战好友**（进本按钮） | `日常/选择助战好友.png` 1.0000 @(532,628) |
  852|     ⚠️ 地图页上那个金色「BONUS」标签和详情页这个「BONUS」标题**不是同一张图**：
  853|     金色标签在详情页上只有 **0.456** 分，所以详情页必须用新模板 `日常/BONUS详情标题.png`

  854|     来认（它在详情页 1.0000、活动地图页 0.4764、装备页 0.4500）。
  855|     这个新模板的另一个用处是**防止点歪进错关** —— 别的关卡详情页也有
  856|     「选择助战好友」「使用双倍券」，只有它能证明"我进的是 BONUS 关"。
  857| 
  858| 59. **`agent/my_bonus.py` 的 `_fight` 现在会处理网络异常**（用户报的「进入战斗会因为网络异常卡住」）：

  860|     - 认到 `通用/网络连接中.png`（实测 1.0000 @(428,302)）就**停下不点**，等它自己好，
  861|       等待的秒数从时间上限里补回来（不算进 `duration`）
  862|     - 「人还在助战页」= 开战按钮还认得到（`日常/开战.png`，**连续两次命中才算**，
  863|       免得把"开战→loading"那一瞬误判）。确认还在就重新点开战，最多 `reentry_limit`(3) 次，
  864|       用完直接收工，不陪它耗满 150 秒

  867| 
  868| 60. **别在"没加载完"的页面上点按钮**（同一个坑的第三次）——
  869|     同一段 pipeline（`选好友 → 点好友 → 助战就绪 → 开战`）我跑了三遍：
  870|     - 12:01 那次：全通
  871|     - 13:0x 两次：`选好友` 命中并点了，**但页面纹丝不动**（连拍 80 帧全是详情页），

  909|     `Select-String gui\interface.json -Pattern '"version"'` 看一眼是不是你要的那个版本。
  910| 
  911| 63. **「虚轴之庭」和「使魔的爱」这两条链从来就没通过**（2026-09-16 实测修正）——
  912|     用户报的「还没打就退出了」，原因是链子里有一个认 `日常/出击.png` 的节点：
  913|     **那张模板在真页面上只有 0.3781 ~ 0.7470 分，一次都没命中过**，而且整页 OCR

  916|     **实测出来的真实路径**（每一张都在真机帧上量过）：
  917|     ```
  918|     战斗 nav → [活动列表]   多元裂缝 卡片 @(752,169)        0.9999
  919|              → [多元裂缝列表] 虚轴之庭 卡片 @(836,232)        1.0000
  920|                              使魔的爱 卡片 @(593,440,115,105)  1.0000   ← 注意是"卡片"不是标题

  921|              → [虚轴之庭地图页] 关卡入口 @(657,492)           （新增节点「点关卡」）
  922|              → [关卡详情页]   快捷战斗 @(900,628)            0.9998
  923|                              选择助战好友 @(532,628)         0.9971
  924|              → [快捷战斗弹窗] 确定 @(488,448) = 快捷战斗确定.png 1.0000（取消在右边 (693,455)）
  925|              → [结算页]      确定 @(716,588) = 结算确定.png   1.0000

  928|     | 模板 | 尺寸 | 该页 | 别的页最高 |
  929|     | --- | --- | --- | --- |
  930|     | `日常/使魔的爱_卡片.png` | 115x105 | 活动列表 1.0000 | 0.46（关卡详情页） |
  931|     | `日常/虚轴之庭_关卡入口.png` | 60x60 | 虚轴之庭地图页 1.0000（多帧同分，那页是静态的） | 0.56（主界面） |
  932|     ⚠️ **使魔的爱 不能按标题文字点**：`使魔的爱_文字.png`（129x30 标题）在列表上是 0.9980，

  934|     （(593,440)-(708,545)）。虚轴之庭同理：列表上点卡片文字进的是**地图页**，
  935|     还要再点一次地图上的关卡入口才到详情页。
  936|     **实测结果**：虚轴之庭连刷 2 次 ✓、使魔的爱刷 1 次 ✓（都是「快捷战斗」→「确定」→「结算确定」）。
  937| 
  938| 64. **「快捷战斗」是免费扫荡，不花体力** —— 关卡详情页右下角写着「今日免费 6/6」

  941| 
  942| 65. **`next` 里"无识别的纯点击"兜底会抢在识别节点前面**（和坑 25 同源，这次踩在模块入口上）——
  943|     `日常_虚轴之庭.next = [进多元, 回列表, 翻页, 开列表(纯 Click，必然命中), ...]`：
  944|     MAA 每轮按顺序试候选，`进多元` 需要等页面加载出来才认得到，而 `开列表` 第一轮就命中，
  945|     于是**每轮都被它吃掉**。修法是给识别节点加**自重试**（把自己放进 `next` 并 `max_hit`），

  948|     才能到活动列表 —— 所以给它自己加了自重试（最多点 3 次，每次 3 秒后再看一遍），
  949|     表现就是：一次点不动/层数不够，会自己再往上点一层，最后才 `逃逸`。
  950|     改完这次两个模块**连着跑**也顺了：`虚轴之庭 开列表→进多元→点卡→点关卡` ，
  951|     `使魔的爱 开列表→点卡（卡片）` ✓（这次两边当日次数都用完了，所以没扫荡，这是对的）。
  952| 

  953| 66. **`bonus_enter` 的导航顺序：必须先回「战斗」分区，再往上层翻**（2026-09-16 踩两次）——
  954|     「战斗」分区会**记住上次停在哪个活动子页**：刷完虚轴之庭之后点「战斗」直接回到
  955|     **多元裂缝列表**，那一页上既没有 BONUS 标签、也没有活动列表的「难度指数/剩余时间」，
  956|     于是 `bonus_enter` 一个锚点都认不到 → 存图收场（表现就是"活动 BONUS 没正常打"）。
  957|     修法是给它加「**点右上角活动名往上一层**」（实测：多元裂缝列表 →(1150,165)→ 活动列表 ✓，

  960|     **「回归赠礼」横幅** —— 那是个**全屏子页、导航栏是死的**，进去就出不来，
  961|     整条链直接报废（我是靠 `am force-stop` + 「启动崩坏学园2」重开游戏才出来的，两次）。
  962|     所以顺序固定成：`BONUS 详情页/活动列表 → 首页+战斗（幂等）→ 再上翻（最多 3 次）`。
  963|     修完实测：连刷 3 轮全通（131/112/91 次点击认到结算），第 3 轮弹「补充体力」→ 点取消收工 ✓。
  964| 

  972|     | --- | --- | --- | --- |
  973|     | 卡住的结算页 | 0.809 ✅(0.78) | 0.903 ✅ | 0.983 ✅ |
  974|     | 虚轴之庭扫荡结算页 | 0.954 ✅ | 0.555 | 1.000 ✅ |
  975|     | 关卡详情页 / 地图页 / 活动列表 / 主界面 / 助战列表 / 快捷战斗弹窗 | 0.29~0.53 | 0.20~0.32 | 0.27~0.40 |
  976|     最后一排说明**误判风险也很低**（离 0.78/0.85 阈值都还有 0.25 以上余量）。

  981|     **~350ms/次**（模板匹配再加 ~0.1s）。所以：
  982|     - 战斗里连点武器的节奏 ≈ `70ms + sleep(click_interval)`；`click_interval` 从 0.16 调到 **0.22**
  983|       （≈3.4 次/秒）—— BONUS 关本身是**自动战斗**，连点只是保险，再密只会把模拟器拖慢。
  984|     - `check_every` 从 3.0 调到 **1.5**：打完那一刻最多等 1.5 秒就发现（原来最多 3 秒）。
  985|     - 真正"慢"的大头其实是**那些 2~6 秒的 post_delay**（等游戏自己的转场/加载），

 1026| ---
 1027| 
 1028| ## 十一、2026-09-16 下午这一轮的实测记录（BONUS 链终于跑通）
 1029| 
 1030| 跑法：`python tools/run_task.py 关卡战斗`（体力 220，BONUS 一轮 50）。

 1031| 跑通那次的节点链（`debug/t9.log`）：
 1032| ```
 1033| 关卡战斗_入口 → 关卡战斗_就绪 → 日常_开始 → 步骤2..6 → 日常_活动BONUS（自定义动作，进详情页）
 1034| → 选好友 → 点好友（助战_备用装备 命中）→ 助战就绪 → 开战 → 打完_自动 → 收尾 → 步骤7 → 收尾
 1035| ```

 1037| ```
 1038| [bonus] 认不出这是哪一页 —— 点「首页」再点「战斗」（幂等操作）
 1039| [bonus] 导航一圈之后认到 BONUS 标签 @(176,240,116,40)，中心 (234,260)
 1040| [bonus]   点「BONUS 入口候选1（偏移 [20, 38]）」(254, 298)
 1041| [bonus] 进 BONUS 详情页了（偏移 [20, 38] 生效）

 1044| [bonus]   认到战斗结算（点了 91 次）
 1045| [bonus]   认到「再次挑战」（体力够）→ 点「再次挑战」→ 到好友列表了（认到「备用装备」那一格）
 1046| [bonus]   点「选备用装备」(464, 255) → 点「开战」[879, 659]
 1047| [bonus] === 第 2/999 轮 ===    …（同样跑通）
 1048| ```

 1053| - 体力耗尽那一刻的表现（`补充体力` 弹窗 → 点取消 → 收尾）—— 这轮刚好没跑到
 1054| - 「使用双倍券」那条路（详情页上显示有 66 张，勾选框在 `使用双倍券.png` 右边 +196）
 1055| - 虚轴之庭 / 使魔的爱那两条链（它们走的是 `日常_开战 → 自动战斗_打到结束`，另一个 action）
 1056| 
 1057| **这一轮改动的文件**（都跑过 `validate_schema` + `check_env`）：

 1059| | 文件 | 改了什么 |
 1060| | --- | --- |
 1061| | `agent/my_bonus.py` | 新增 `bonus_enter`（认内容找入口，旧名 `bonus_find_entry` 保留兼容）；`_fight` 加网络等待 / 开战重试 / 失败存图；`_next_round` 修「好友列表」判据、加 `_wait_net`；`_Ctl` 加 `save_shot` |
 1062| | `assets/resource/pipeline/30_清日常.json` | `日常_活动BONUS` / `点左上` / `点左上2` 换成自定义动作；8 处 `通用/主界面.png` 阈值 0.75 → 0.62 |
 1063| | `assets/resource/pipeline/00_通用.json` | `主界面_就绪` / `回到主界面` 阈值 0.75 → 0.62 |

 1067| | `tools/ascii_view.py` | 新增：截图转字符画 |
 1068| | `tools/template_health.py` | 修 `UnboundLocalError` |
 1069| | `tools/templates.json` | 更新 `活动_BONUS` 说明、登记新模板 |
 1070| | `agent/battle_config.json` | 新增网络等待 / 入口候选偏移 / 导航点击点等参数 |
